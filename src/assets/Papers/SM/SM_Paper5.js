export const SM_paper5 = [
  {
    id: "S11q1",
    domain: "Security",
    text: "An EC2 instance has an IAM instance role attached to it, providing it read and write access to the S3 bucket 'my_bucket'. You have tested the IAM instance role and both reads and writes are working. You then remove the IAM role from the EC2 instance and test both read and write again. Writes stopped working but reads are still working.\n\nWhat is the likely cause of this behavior?",
    answers: [
      "The S3 bucket policy authorizes reads",
      "The EC2 instance is using cached temporary IAM credentials",
      "When a read is done on a bucket, there's a grace period of 5 minutes to do the same read again",
      "Removing an instance role from an EC2 instance can take a few minutes before being active",
    ],
    correctAnswerIndex: 0,
    correctAnswerIndices: [0],
    isMultiSelect: false,
    explanation:
      "In AWS, access to S3 is governed by a combination of IAM policies (identity-based) and S3 Bucket Policies (resource-based). When the IAM role was removed, the identity-based permissions were revoked, causing writes to fail. However, if the S3 bucket has a bucket policy that allows public read access or specific access to the EC2 instance's IP/account, those 'reads' will continue to work regardless of the instance's IAM role status.",
    answerExplanations: [
      "Correct. S3 bucket policies can grant access independently of IAM roles. If the bucket policy allows read access to the resource (e.g., public read or specific VPC endpoint access), the instance can still read data even without an attached role.",
      "Incorrect. While IAM roles use temporary credentials, removing the role from the instance profile immediately prevents the metadata service from providing those credentials. If caching were the issue, both reads and writes would likely still work for a short period.",
      "Incorrect. There is no 'grace period' for S3 reads. Every request is independently authenticated and authorized in real-time.",
      "Incorrect. IAM role detachments are processed almost instantly. Furthermore, if propagation delay were the issue, it would affect both reads and writes equally, not just one of them.",
    ],
  },
  {
    id: "S11q2",
    domain: "Deployment",
    text: "A company has hosted its restaurant review website on an Amazon EC2 instance. The website supports multiple languages and the preferred language is added as a query string parameter to the request. The directory structure and file names for all versions of the website are identical. The website responds with the chosen language's webpage when accessed directly. However, when the same webpage is accessed through the configured CloudFront distribution, it defaults to a single language.\n\nHow will you fix this issue?",
    answers: [
      "Create a new cache policy for the CloudFront distribution and set the cache behavior to Query string forwarding and caching. In the Query string whitelist field include the language string. Update the CloudFront distribution to use the new cache policy",
      "Create a new cache policy for the CloudFront distribution and set the cache behavior to Cache based on selected request headers. Use Whitelist Headers as the caching criteria",
      "Create a new cache policy for the CloudFront distribution and set the cache behavior to None to improve caching performance. Update the CloudFront distribution to use the new cache policy",
      "Choose the Customize option for the Object Caching setting and reduce the Default TTL value so that CloudFront forwards requests to your origin more frequently",
    ],
    correctAnswerIndex: 0,
    correctAnswerIndices: [0],
    isMultiSelect: false,
    explanation:
      "By default, Amazon CloudFront does not consider query strings when caching objects. This means if two users request 'index.html?lang=en' and 'index.html?lang=fr', CloudFront treats them as the same object and serves the cached version of whichever was requested first. To fix this, you must configure a Cache Policy that includes the specific query string parameter (e.g., 'lang') in the cache key. This ensures CloudFront caches and serves separate versions of the file based on the language parameter value.",
    answerExplanations: [
      "Correct. Adding the query string parameter to the cache policy allows CloudFront to distinguish between different language versions of the same file path and cache them separately.",
      "Incorrect. The scenario specifically mentions that the language is determined by a query string parameter, not an HTTP request header (like Accept-Language).",
      "Incorrect. Setting query string forwarding to 'None' (the default) tells CloudFront to ignore query strings entirely when caching, which is exactly what is causing the current problem.",
      "Incorrect. Reducing the TTL might make CloudFront check the origin more often, but it doesn't solve the underlying issue that CloudFront is incorrectly grouping different language requests into a single cache entry.",
    ],
  },
  {
    id: "S11q3",
    domain: "Development with AWS Services",
    text: "A data analytics company ingests a large number of messages and stores them in an RDS database using Lambda. Because of the increased payload size, it is taking more than 15 minutes to process each message.\n\nAs a Developer Associate, which of the following options would you recommend to process each message in the MOST scalable way?",
    answers: [
      "Provision an EC2 instance to poll the messages from an SQS queue",
      "Provision EC2 instances in an Auto Scaling group to poll the messages from an SQS queue",
      "Contact AWS Support to increase the Lambda timeout to 60 minutes",
      "Use DynamoDB instead of RDS as database",
    ],
    correctAnswerIndex: 1,
    correctAnswerIndices: [1],
    isMultiSelect: false,
    explanation:
      "AWS Lambda has a hard maximum timeout limit of 15 minutes (900 seconds) per execution. If a task consistently requires more than 15 minutes, it is no longer a candidate for Lambda. Moving the workload to EC2 instances allows for long-running processes. To ensure the solution is 'most scalable,' these instances should be managed by an Auto Scaling group (ASG) that can adjust the number of instances based on the SQS queue depth (using tracking metrics like ApproximateNumberOfMessagesVisible).",
    answerExplanations: [
      "Incorrect. While using an EC2 instance removes the 15-minute timeout restriction, a single instance is a single point of failure and does not scale to handle high volumes of messages.",
      "Correct. This approach removes the execution time limit and provides horizontal scalability. The Auto Scaling group can scale in or out based on the demand of the SQS queue.",
      "Incorrect. The 15-minute timeout for AWS Lambda is a hard service quota and cannot be increased by AWS Support.",
      "Incorrect. Changing the database backend does not solve the compute layer's timeout issue. The bottleneck is the processing time of the Lambda function, not the database write speed.",
    ],
  },
  {
    id: "S11q4",
    text: 'A developer has defined a Lambda integration in Amazon API Gateway using a stage variable. However, when the developer invokes the API method, it consistently returns an "Internal server error" and a 500 status code.\n\nWhat steps should the developer take to fix the issue?',
    answers: [
      "If you create a stage variable to call a Lambda function through your API, you must add the required permissions. Update your Lambda function's resource-based AWS Identity and Access Management (IAM) policy so that it grants invoke permission to the API Gateway",
      "When setting the Lambda function as the value of a stage variable, use the function's ARN and not the function alias for setting up the value",
      "If you create a stage variable to call a function through your API, you must add the required permissions. Create an IAM role that your Lambda function can assume when invoking the respective AWS resources",
      "API calls can't exceed the maximum allowed API request rate per account and per Region. Implement error retries and exponential backoffs to fix the error",
    ],
    correctAnswerIndex: 0,
    correctAnswerIndices: [0],
    isMultiSelect: false,
    explanation:
      "When you use the API Gateway console to link a Lambda function directly, the console automatically adds the necessary resource-based permissions. However, when you use a stage variable to define the Lambda function, API Gateway cannot determine which specific function will be called at runtime. Therefore, you must manually grant API Gateway permission to invoke the function by updating the Lambda function's resource-based policy or by using an IAM execution role for the API.",
    answerExplanations: [
      "Correct. API Gateway requires explicit 'lambda:InvokeFunction' permission to call your Lambda function. Since stage variables are dynamic, this permission must be added manually via the Lambda resource-based policy.",
      "Incorrect. While you can use an ARN, the standard practice for stage variables often involves using function names or aliases. The core issue of 500 errors in this context is almost always related to missing invocation permissions.",
      "Incorrect. The Lambda function's execution role allows it to access other AWS services (like DynamoDB). The problem here is the permission for API Gateway to call the Lambda function, not the Lambda's permission to call other resources.",
      "Incorrect. Rate limiting issues result in a 429 'Too Many Requests' error, not a 500 Internal Server Error. A 500 error typically indicates an unhandled exception or a configuration/permission failure.",
    ],
  },
  {
    id: "S11q5",
    domain: "Development with AWS Services",
    text: "A company has recently launched a new gaming application that the users are adopting rapidly. The company uses RDS MySQL as the database. The development team wants an urgent solution to this issue where the rapidly increasing workload might exceed the available database storage.\n\nAs a developer associate, which of the following solutions would you recommend so that it requires minimum development effort to address this requirement?",
    answers: [
      "Migrate RDS MySQL database to DynamoDB which automatically allocates storage space when required",
      "Migrate RDS MySQL database to Aurora which offers storage auto-scaling",
      "Enable storage auto-scaling for RDS MySQL",
      "Create read replica for RDS MySQL",
    ],
    correctAnswerIndex: 2,
    correctAnswerIndices: [2],
    isMultiSelect: false,
    explanation:
      "Amazon RDS Storage Auto Scaling is the most efficient solution as it requires zero downtime and minimal configuration. When enabled, RDS monitors the actual storage consumption and automatically increases the allocated storage capacity when it detects that the database is running out of space (specifically when free space is less than 10% of allocated storage and the condition lasts at least five minutes). This allows the database to grow seamlessly without manual intervention or migration efforts.",
    answerExplanations: [
      "Incorrect. Migrating from a relational database (MySQL) to a NoSQL database (DynamoDB) would require massive development effort to rewrite the application's data access layer and schema design.",
      "Incorrect. While Amazon Aurora does provide automatic storage scaling (up to 128 TiB), migrating the existing RDS MySQL database to Aurora involves more administrative and systems effort compared to simply clicking a checkbox in the existing RDS instance settings.",
      "Correct. Enabling Storage Auto Scaling is a built-in feature of Amazon RDS for MySQL. It requires the least effort (no migration, no code changes) and directly addresses the urgent need for more storage.",
      "Incorrect. Read replicas are used to scale read traffic and improve performance for read-heavy workloads. They do not increase the storage capacity of the primary (source) database or solve 'out of space' issues.",
    ],
  },
  {
    id: "S11q6",
    domain: "Deployment",
    text: "Which environment variable can be used by AWS X-Ray SDK to ensure that the daemon is correctly discovered on ECS?",
    answers: [
      "AWS_XRAY_DAEMON_ADDRESS",
      "AWS_XRAY_TRACING_NAME",
      "AWS_XRAY_CONTEXT_MISSING",
      "AWS_XRAY_DEBUG_MODE",
    ],
    correctAnswerIndex: 0,
    correctAnswerIndices: [0],
    isMultiSelect: false,
    explanation:
      "The AWS_XRAY_DAEMON_ADDRESS environment variable is used to specify the host and port of the X-Ray daemon listener. While the SDK defaults to 127.0.0.1:2000, in an Amazon ECS environment where the daemon might be running in a separate sidecar container or as a service on a different host (especially in bridge networking mode), this variable is essential for pointing the SDK to the correct address so traces can be successfully relayed.",
    answerExplanations: [
      "Correct. This variable tells the X-Ray SDK where to send segment data. In ECS, if you use a sidecar or a separate daemon service, you set this to 'container_name:port' or the specific IP/host where the daemon is reachable.",
      "Incorrect. AWS_XRAY_TRACING_NAME is used to set the default service name for the application segments, but it does not assist in discovering the X-Ray daemon.",
      "Incorrect. AWS_XRAY_CONTEXT_MISSING determines how the SDK behaves when a segment is missing (e.g., LOG_ERROR or RUNTIME_ERROR), which is unrelated to network discovery of the daemon.",
      "Incorrect. AWS_XRAY_DEBUG_MODE (or AWS_XRAY_LOG_LEVEL) is used to enable more verbose logging within the SDK for troubleshooting, but it doesn't configure the daemon's connection string.",
    ],
  },
  {
    id: "S11q7",
    domain: "Development with AWS Services",
    text: "When your company first created an AWS account, you began with a single sign-in principal called a root user account that had complete access to all AWS services and resources.\n\nWhat should you do to adhere to best practices for using the root user account?",
    answers: [
      "It should be accessible by 3 to 6 members of the IT team",
      "It should be accessible by no one, throw away the passwords after creating the account",
      "It should be accessible by one admin only after enabling Multi-factor authentication",
      "It should be accessible using the access key id and secret access key",
    ],
    correctAnswerIndex: 2,
    correctAnswerIndices: [2],
    isMultiSelect: false,
    explanation:
      "The AWS root user is the most privileged identity in an AWS account. Best practices dictate that you should lock away root user credentials and use them only for the few tasks that specifically require root access (such as changing account settings or closing the account). To secure it, you should enable Multi-Factor Authentication (MFA) and ensure only a single, highly trusted administrator has access to the credentials.",
    answerExplanations: [
      "Incorrect. Sharing root access among multiple team members is a major security risk. Instead, you should create individual IAM users with administrative permissions for IT team members.",
      "Incorrect. While you should rarely use the root account, you cannot 'throw away' the password. You must maintain access for critical account management tasks that only the root user can perform.",
      "Correct. Enabling MFA is a critical security layer for the root user. Access should be restricted to a single administrator to maintain accountability and minimize the attack surface.",
      "Incorrect. AWS strongly recommends against creating or using access keys for the root user. If you need programmatic access, create an IAM user with the necessary permissions and use those access keys instead.",
    ],
  },
  {
    id: "S11q8",
    domain: "Deployment",
    text: "A financial services company has developed a REST API which is deployed in an Auto Scaling Group behind an Application Load Balancer. The API stores the data payload in DynamoDB and the static content is served through S3. Upon analyzing the usage pattern, it's found that 80% of the read requests are shared across all users.\n\nAs a Developer Associate, how can you improve the application performance while optimizing the cost with the least development effort?",
    answers: [
      "Enable ElastiCache Redis for DynamoDB and CloudFront for S3",
      "Enable DAX for DynamoDB and ElastiCache Memcached for S3",
      "Enable DynamoDB Accelerator (DAX) for DynamoDB and CloudFront for S3",
      "Enable ElastiCache Redis for DynamoDB and ElastiCache Memcached for S3",
    ],
    correctAnswerIndex: 2,
    correctAnswerIndices: [2],
    isMultiSelect: false,
    explanation:
      "To meet the requirements with the least development effort, Amazon DynamoDB Accelerator (DAX) and Amazon CloudFront are the ideal choices. DAX is a managed, in-memory cache specifically for DynamoDB that is API-compatible; you only need to change the DynamoDB client in your code to the DAX client. For static content in S3, CloudFront is the native CDN that caches content at edge locations, significantly reducing latency and decreasing S3 egress costs without requiring code changes to the storage layer.",
    answerExplanations: [
      "Incorrect. While Redis can cache DynamoDB data, it is a 'side-cache' that requires significant development effort to implement custom logic for cache invalidation, data fetching, and population (Lazy Loading/Write-through).",
      "Incorrect. ElastiCache (Memcached or Redis) is not a native solution for caching S3 static content. CloudFront is the standard and most efficient way to cache S3 objects.",
      "Correct. DAX provides a seamless 'read-through' cache for DynamoDB with microsecond latency, and CloudFront provides a global CDN for S3 content. Both represent the 'AWS native' way to scale these services with minimal code changes.",
      "Incorrect. This combination uses ElastiCache for both components, which maximizes development overhead and misapplies the technology for S3 content delivery.",
    ],
  },

  {
    id: "S11q9",
    domain: "Deployment",
    text: "An IT company uses AWS CloudFormation templates to provision their AWS infrastructure for Amazon EC2, Amazon VPC, and Amazon S3 resources. Using cross-stack referencing, a developer creates a stack called NetworkStack which will export the subnetId that can be used when creating EC2 instances in another stack.\n\nTo use the exported value in another stack, which of the following functions must be used?",
    answers: ["!Ref", "!GetAtt", "!ImportValue", "!Sub"],
    correctAnswerIndex: 2,
    correctAnswerIndices: [2],
    isMultiSelect: false,
    explanation:
      "To share resources across CloudFormation stacks, you must first 'Export' the value in the source stack's Outputs section with a unique name. To consume that value in a different stack (within the same region and account), you use the intrinsic function `Fn::ImportValue` (or the YAML short-form `!ImportValue`). This creates a dependency between the stacks, preventing the source stack from being deleted or the exported value from being modified while the second stack is still using it.",
    answerExplanations: [
      "Incorrect. The `!Ref` function is used to get the value of a parameter or a resource defined *within the same* template, not for cross-stack referencing.",
      "Incorrect. The `!GetAtt` function retrieves the value of an attribute from a resource *within the same* template (e.g., getting the PrivateIp of an EC2 instance).",
      "Correct. `!ImportValue` is specifically designed to return the value of an output exported by another stack, enabling cross-stack communication.",
      "Incorrect. The `!Sub` (Substitute) function is used to replace variables in a string with specified values, often used for constructing ARNs or configuration strings, but it cannot fetch exported values by itself.",
    ],
  },
  {
    id: "S11q10",
    domain: "Development with AWS Services",
    text: "You are using AWS SQS FIFO queues to get the ordering of messages on a per user_id basis. On top of this, you would like to make sure that duplicate messages should not be sent to SQS as this would cause application failure.\n\nAs a developer, which message parameter should you set for deduplicating messages?",
    answers: [
      "MessageGroupId",
      "MessageDeduplicationId",
      "ReceiveRequestAttemptId",
      "ContentBasedDeduplication",
    ],
    correctAnswerIndex: 1,
    correctAnswerIndices: [1],
    isMultiSelect: false,
    explanation:
      "In an SQS FIFO queue, the `MessageDeduplicationId` is the specific parameter used to prevent duplicate messages from being processed. If a message with a particular deduplication ID is sent successfully, any subsequent messages with the same ID sent within a 5-minute deduplication window are accepted by SQS but not delivered to consumers. This ensures 'exactly-once' delivery at the sender level.",
    answerExplanations: [
      "Incorrect. `MessageGroupId` is used to group messages together to ensure they are processed in order relative to each other (in this case, by `user_id`). It does not handle deduplication.",
      "Correct. This is the token used for deduplication of sent messages. It ensures that if the same message is sent multiple times due to retries or errors, it is only stored and delivered once.",
      "Incorrect. `ReceiveRequestAttemptId` is used for deduplicating 'ReceiveMessage' calls (consumer side) to ensure a consumer gets the same set of messages if a network error occurs during the receive call.",
      "Incorrect. While related, `ContentBasedDeduplication` is a queue-level setting (not a message parameter) that tells SQS to automatically generate a `MessageDeduplicationId` based on a hash of the message body.",
    ],
  },
  {
    id: "S11q11",
    domain: "Deployment",
    text: "Your team lead has finished creating a CodeBuild project in the management console and a build spec has been defined for the project. After the build is run, CodeBuild fails to pull a Docker image into the build environment.\n\nWhat is the most likely cause?",
    answers: [
      "The Docker image is missing some tags",
      "Missing IAM permissions for the CodeBuild Service",
      "CodeBuild cannot work with custom Docker images",
      "The Docker image is too big",
    ],
    correctAnswerIndex: 1,
    correctAnswerIndices: [1],
    isMultiSelect: false,
    explanation:
      "When AWS CodeBuild attempts to pull a container image (whether from Amazon ECR or a private registry), it uses the permissions granted to its **Service Role**. If this role lacks the necessary permissions (such as `ecr:BatchGetImage` and `ecr:GetDownloadUrlForLayer` for ECR), the pull request will fail. While the console often automates role creation, manual changes or specific custom image scenarios frequently lead to missing IAM permissions.",
    answerExplanations: [
      "Incorrect. While an incorrect tag would cause a 'manifest not found' error, missing optional tags does not prevent an image from being pulled if a valid default (like 'latest') or specific tag is requested.",
      "Correct. CodeBuild requires explicit permission via its IAM service role to access the registry where the image is stored. This is the most common cause of pull failures during the provisioning phase.",
      "Incorrect. CodeBuild explicitly supports custom Docker images from Amazon ECR, Docker Hub, and other private registries to allow for tailored build environments.",
      "Incorrect. While very large images may slow down the 'Provisioning' phase or exceed disk space during the build, they do not inherently cause a 'failure to pull' unless they exceed specific service timeouts or storage limits, which is rarely the primary cause compared to permissions.",
    ],
  },
  {
    id: "S11q12",
    domain: "Deployment",
    text: "Your company wants to move away from manually managing Lambda in the AWS console and wants to upload and update them using AWS CloudFormation.\n\nHow do you declare an AWS Lambda function in CloudFormation? (Select two)",
    answers: [
      "Upload all the code as a zip to S3 and refer the object in AWS::Lambda::Function block",
      "Upload all the code as a folder to S3 and refer the folder in AWS::Lambda::Function block",
      "Write the AWS Lambda code inline in CloudFormation in the AWS::Lambda::Function block as long as there are no third-party dependencies",
      "Write the AWS Lambda code inline in CloudFormation in the AWS::Lambda::Function block and reference the dependencies as a zip file stored in S3",
      "Upload all the code to CodeCommit and refer to the CodeCommit Repository in AWS::Lambda::Function block",
    ],
    correctAnswerIndex: null,
    correctAnswerIndices: [0, 2],
    isMultiSelect: true,
    explanation:
      "In AWS CloudFormation, the `AWS::Lambda::Function` resource's `Code` property supports two primary methods for providing function code. The first is referencing a .zip file stored in an Amazon S3 bucket. The second is the `ZipFile` property (specifically for Node.js and Python runtimes), which allows you to write the source code directly in the template. Note that inline code is limited to 4096 characters and cannot include binary dependencies.",
    answerExplanations: [
      "Correct. This is the standard method for deploying Lambda functions with dependencies. You specify the S3Bucket, S3Key, and optionally S3ObjectVersion.",
      "Incorrect. Lambda deployment packages must be single .zip or .jar files; CloudFormation cannot reference a raw folder structure in S3.",
      "Correct. This is useful for simple scripts (e.g., custom CloudFormation resources). It is only available for interpreted languages like Python and Node.js and must be placed under the `ZipFile` key.",
      "Incorrect. CloudFormation does not support a hybrid model where part of the code is inline and part is pulled from S3. You must use one or the other for the main code block.",
      "Incorrect. While you can use CodeCommit to trigger a CI/CD pipeline (like CodePipeline), the `AWS::Lambda::Function` resource cannot directly point to a CodeCommit repository to fetch code.",
    ],
  },
  {
    id: "S11q13",
    domain: "Development with AWS Services",
    text: "You are using AWS SQS FIFO queues to get the ordering of messages on a per user_id basis.\n\nAs a developer, which message parameter should you set the value of user_id to guarantee the ordering?",
    answers: [
      "MessageHash",
      "MessageOrderId",
      "MessageGroupId",
      "MessageDeduplicationId",
    ],
    correctAnswerIndex: 2,
    correctAnswerIndices: [2],
    isMultiSelect: false,
    explanation:
      "In SQS FIFO queues, the `MessageGroupId` is the parameter used to ensure that messages are processed in a specific order. When multiple messages have the same `MessageGroupId`, SQS ensures they are delivered to consumers in the exact order they arrived at the queue. By setting this to `user_id`, you guarantee that all messages for a specific user are processed sequentially, while messages for different users (different Group IDs) can be processed in parallel by multiple consumers.",
    answerExplanations: [
      "Incorrect. This is not a standard SQS message parameter used for ordering.",
      "Incorrect. This parameter does not exist in the AWS SQS API.",
      "Correct. The `MessageGroupId` is the required parameter for grouping messages that must be processed in a strict linear order within a FIFO queue.",
      "Incorrect. `MessageDeduplicationId` is used to prevent the same message from being sent or processed more than once within a 5-minute window; it does not govern the sequence of different messages.",
    ],
  },
  {
    id: "S11q14",
    domain: "Troubleshooting and Optimization",
    text: "You have been collecting AWS X-Ray traces across multiple applications and you would now like to index your X-Ray traces to search and filter through them efficiently.\n\nWhat should you use in your instrumentation?",
    answers: ["Annotations", "Metadata", "Segments", "Sampling"],
    correctAnswerIndex: 0,
    correctAnswerIndices: [0],
    isMultiSelect: false,
    explanation:
      "In AWS X-Ray, Annotations are key-value pairs that are indexed by the service. This indexing allows you to use Filter Expressions in the X-Ray console or via the API to quickly find specific traces (e.g., searching for a specific 'game_id' or 'user_type'). Metadata, while also being key-value pairs, is not indexed and is used for storing additional debugging data that doesn't need to be searchable.",
    answerExplanations: [
      "Correct. Annotations are indexed key-value pairs used for searching and filtering traces.",
      "Incorrect. Metadata allows for complex data types (objects, lists) but is not indexed and cannot be used for filtering traces.",
      "Incorrect. Segments represent the work done by a single compute resource (like a Lambda function or EC2 instance); they contain the annotations but are not the mechanism for indexing specifically.",
      "Incorrect. Sampling is the process of determining which requests are traced to control costs and overhead; it does not handle the indexing of data within those traces.",
    ],
  },
  {
    id: "S11q15",
    domain: "Development with AWS Services",
    text: "Which of the following CLI options will allow you to retrieve a subset of the attributes coming from a DynamoDB scan?",
    answers: [
      "--projection-expression",
      "--filter-expression",
      "--page-size",
      "--max-items",
    ],
    correctAnswerIndex: 0,
    correctAnswerIndices: [0],
    isMultiSelect: false,
    explanation:
      "To optimize data transfer and reduce the size of the payload returned from a DynamoDB `Scan` or `Query`, you use a Projection Expression. By default, DynamoDB returns all attributes of the items found. A `--projection-expression` allows you to specify exactly which attributes you want (e.g., just 'UserId' and 'Email'), which can help stay within the 6MB limit for a single response, though it does not reduce the Read Capacity Units (RCU) consumed.",
    answerExplanations: [
      "Correct. This option defines a string that identifies the specific attributes you want to retrieve from the table.",
      "Incorrect. A filter expression is used to include or exclude items based on their values *after* the scan has read them from the disk. It filters whole items, not specific attributes within those items.",
      "Incorrect. This is a pagination setting for the AWS CLI. it controls the number of items retrieved in each internal API call but does not affect which attributes are returned.",
      "Incorrect. This limits the total number of items the CLI will output from the overall command execution, but it still retrieves all attributes for those items.",
    ],
  },
  {
    id: "S11q16",
    domain: "Deployment",
    text: "Your organization has set up a full CI/CD pipeline leveraging CodePipeline and the deployment is done on Elastic Beanstalk. This pipeline has worked for over a year now but you are approaching the limits of Elastic Beanstalk in terms of how many versions can be stored in the service.\n\nHow can you remove older versions that are not used by Elastic Beanstalk so that new versions can be created for your applications?",
    answers: [
      "Use Worker Environments",
      "Setup an .ebextensions file",
      "Define a Lambda function",
      "Use a Lifecycle Policy",
    ],
    correctAnswerIndex: 3,
    correctAnswerIndices: [3],
    isMultiSelect: false,
    explanation:
      "AWS Elastic Beanstalk has a default limit on the number of application versions (usually 1,000). To manage this automatically, you can set up an Application Version Lifecycle Policy. This policy allows you to define rules to delete old versions based on either the total number of versions (quota) or the age of the versions. This ensures that the deployment pipeline doesn't break due to reaching service limits.",
    answerExplanations: [
      "Incorrect. Worker environments are used for background processing and long-running tasks; they have no impact on application version management.",
      "Incorrect. While `.ebextensions` files are used to configure the environment's resources and software, they are not designed to manage the global lifecycle of application versions stored in the service.",
      "Incorrect. While you could technically write a custom Lambda script to delete versions via the AWS SDK, it represents high development overhead compared to the built-in, native Lifecycle Policy feature.",
      "Correct. Lifecycle policies are the native way to automate the deletion of old application versions in Elastic Beanstalk, preventing you from hitting the version limit.",
    ],
  },
  {
    id: "S11q16",
    domain: "Deployment",
    text: "Your organization has set up a full CI/CD pipeline leveraging CodePipeline and the deployment is done on Elastic Beanstalk. This pipeline has worked for over a year now but you are approaching the limits of Elastic Beanstalk in terms of how many versions can be stored in the service.\n\nHow can you remove older versions that are not used by Elastic Beanstalk so that new versions can be created for your applications?",
    answers: [
      "Use Worker Environments",
      "Setup an .ebextensions file",
      "Define a Lambda function",
      "Use a Lifecycle Policy",
    ],
    correctAnswerIndex: 3,
    correctAnswerIndices: [3],
    isMultiSelect: false,
    explanation:
      "AWS Elastic Beanstalk has a default limit of 1,000 application versions per account. To automate the removal of old versions, you should implement an Application Version Lifecycle Policy. This policy can be configured to trigger whenever a new version is created, deleting either the oldest versions (based on a total count limit) or versions older than a specific number of days. This prevents deployment failures caused by reaching the service quota.",
    answerExplanations: [
      "Incorrect. Worker environments are intended for offloading long-running HTTP requests or background tasks and do not manage application version storage.",
      "Incorrect. While `.ebextensions` can configure environment-specific resources, application version management is a global application-level setting managed through lifecycle policies.",
      "Incorrect. Although a Lambda function using the AWS SDK could technically delete versions, it is an over-engineered solution compared to the native Lifecycle Policy feature provided by Elastic Beanstalk.",
      "Correct. Lifecycle policies are the built-in mechanism for managing application version quotas, allowing for automatic deletion based on age or the total number of versions.",
    ],
  },
  {
    id: "S11q18",
    domain: "Development with AWS Services",
    text: "A company ingests real-time data into its on-premises data center and subsequently a daily data feed is compressed into a single file and uploaded on Amazon S3 for backup. The typical compressed file size is around 2 GB.\n\nWhich of the following is the fastest way to upload the daily compressed file into S3?",
    answers: [
      "Upload the compressed file using multipart upload",
      "FTP the compressed file into an EC2 instance that runs in the same region as the S3 bucket. Then transfer the file from the EC2 instance into the S3 bucket",
      "Upload the compressed file using multipart upload with S3 transfer acceleration",
      "Upload the compressed file in a single operation",
    ],
    correctAnswerIndex: 2,
    correctAnswerIndices: [2],
    isMultiSelect: false,
    explanation:
      "To achieve the maximum possible upload speed for a large file (2 GB) from an on-premises location to S3, you should combine two features: Multipart Upload and S3 Transfer Acceleration. Multipart upload allows you to upload parts of the file in parallel, maximizing bandwidth usage and providing better resilience to network failures. S3 Transfer Acceleration speeds up the transfer by routing traffic through Amazon CloudFront's globally distributed Edge Locations, utilizing the optimized AWS private network backbone instead of the public internet.",
    answerExplanations: [
      "Incorrect. While multipart upload improves speed by allowing parallel part transfers, it still relies on the public internet path from on-premises to the S3 bucket's region.",
      "Incorrect. This adds unnecessary complexity and an extra hop. FTP is also generally less efficient and secure than native AWS transfer methods.",
      "Correct. This is the optimal combination. S3 Transfer Acceleration optimizes the network path via AWS Edge Locations, while Multipart Upload parallelizes the data transmission.",
      "Incorrect. For files larger than 100 MB, a single operation is slower and more prone to failure. If a single upload fails at 1.9 GB, you must restart from the beginning, whereas multipart upload only requires re-sending the failed part.",
    ],
  },
  {
    id: "S11q19",
    domain: "Security",
    text: "You were assigned to a project that requires the use of the AWS CLI to build a project with AWS CodeBuild. Your project's root directory includes the buildspec.yml file to run build commands and would like your build artifacts to be automatically encrypted at the end.\n\nHow should you configure CodeBuild to accomplish this?",
    answers: [
      "Use an AWS Lambda Hook",
      "Specify a KMS key to use",
      "Use In Flight encryption (SSL)",
      "Use the AWS Encryption SDK",
      "Use a custom script in buildspec.yml to encrypt files manually",
    ],
    correctAnswerIndex: 1,
    correctAnswerIndices: [1],
    isMultiSelect: false,
    explanation:
      "AWS CodeBuild integrates natively with AWS Key Management Service (KMS) to encrypt build artifacts. When you define a CodeBuild project, you can specify a KMS customer master key (CMK) to encrypt the output artifacts. If you don't specify a custom key, CodeBuild defaults to using the AWS-managed key for Amazon S3 (the service where artifacts are typically stored). This encryption happens automatically as part of the artifact upload process at the end of the build.",
    answerExplanations: [
      "Incorrect. Lambda hooks are typically used in AWS CodeDeploy to perform lifecycle validation tests, not for artifact encryption in CodeBuild.",
      "Correct. By specifying a KMS key (either the default AWS-managed key or a customer-managed CMK), CodeBuild will ensure that all build output artifacts are encrypted before being stored in the S3 bucket.",
      "Incorrect. In-flight encryption (SSL/TLS) protects data as it moves across the network. The question asks about encrypting the resulting artifacts (encryption at rest).",
      "Incorrect. The AWS Encryption SDK is a library used within application code to encrypt/decrypt data. CodeBuild provides managed, transparent encryption for artifacts, so using a separate SDK is unnecessary and adds development overhead.",
      "Incorrect. While you could manually encrypt files using a script, it is not the recommended 'AWS way' when a managed service like KMS is natively integrated into the CodeBuild configuration.",
    ],
  },
  {
    id: "S11q20",
    domain: "Development with AWS Services",
    text: "You are working with a t2.small instance bastion host that has the AWS CLI installed to help manage all the AWS services installed on it. You would like to know the security group and the instance id of the current instance.\n\nWhich of the following will help you fetch the needed information?",
    answers: [
      "Query the user data at http://169.254.169.254/latest/user-data",
      "Create an IAM role and attach it to your EC2 instance that helps you perform a 'describe' API call",
      "Query the metadata at http://169.254.169.254/latest/meta-data",
      "Query the user data at http://254.169.254.169/latest/meta-data",
    ],
    correctAnswerIndex: 2,
    correctAnswerIndices: [2],
    isMultiSelect: false,
    explanation:
      "Instance Metadata Service (IMDS) provides data about your running EC2 instance that you can use to configure or manage the instance. By querying the link-local IP address `169.254.169.254`, you can access various categories of information, including the instance-id, public/private IP addresses, and security groups, without needing IAM permissions or external API calls.",
    answerExplanations: [
      "Incorrect. User data is used to provide scripts or configuration parameters to an instance at launch time (bootstrapping); it does not contain system-generated information like the instance ID or security groups unless you manually placed them there.",
      "Incorrect. While you could use `aws ec2 describe-instances`, this command typically requires the instance ID as a parameter to be efficient. Furthermore, querying metadata is the more direct, standard, and permission-less way to get information about the *local* host.",
      "Correct. This is the correct URI for the Instance Metadata Service. Specific paths like `.../meta-data/instance-id` and `.../meta-data/security-groups` provide the exact information requested.",
      "Incorrect. This option uses an incorrect IP address. The standard link-local address for AWS instance metadata is always `169.254.169.254`.",
    ],
  },
  {
    id: "S11q21",
    domain: "Development with AWS Services",
    text: "The development team at an e-commerce company is preparing for the upcoming Thanksgiving sale. The product manager wants the development team to implement appropriate caching strategy on Amazon ElastiCache to withstand traffic spikes on the website during the sale. A key requirement is to facilitate consistent updates to the product prices and product description, so that the cache never goes out of sync with the backend.\n\nAs a Developer Associate, which of the following solutions would you recommend for the given use-case?",
    answers: [
      "Use a caching strategy to write to the backend first and then invalidate the cache",
      "Use a caching strategy to write to the cache directly and sync the backend at a later time",
      "Use a caching strategy to write to the backend first and wait for the cache to expire via TTL",
      "Use a caching strategy to update the cache and the backend at the same time",
    ],
    correctAnswerIndex: 0,
    correctAnswerIndices: [0],
    isMultiSelect: false,
    explanation:
      "To ensure the cache never goes out of sync with the backend (strong consistency), the application should update the database first and then immediately invalidate the corresponding cache key. This forces the next read request to fetch the fresh data from the database and repopulate the cache (Lazy Loading), ensuring that stale product prices are never displayed to customers during a high-stakes sale.",
    answerExplanations: [
      "Correct. Invalidation ensures that the next request will see the new data from the database. This is the most reliable way to prevent stale data when updates occur.",
      "Incorrect. This describes a 'Write-Back' or 'Write-Behind' strategy, which risks data loss if the cache fails before the backend is synced and results in temporary inconsistency.",
      "Incorrect. While simple, the cache remains stale for the entire duration of the TTL. In an e-commerce setting, showing an old price for even a few minutes can lead to customer dissatisfaction or financial loss.",
      "Incorrect. It is technically impossible to update two independent distributed systems (ElastiCache and RDS) simultaneously in a single atomic transaction without complex distributed locking, which is not a standard feature of ElastiCache.",
    ],
  },
  {
    id: "S11q22",
    domain: "Deployment",
    text: "Your company likes to operate multiple AWS accounts so that teams have their environments. Services deployed across these accounts interact with one another, and now there's a requirement to implement X-Ray traces across all your applications deployed on EC2 instances and AWS accounts.\n\nAs such, you would like to have a unified account to view all the traces. What should you do in your X-Ray daemon set up to make this work? (Select two)",
    answers: [
      "Create a role in the target unified account and allow roles in each sub-account to assume the role.",
      "Enable Cross Account collection in the X-Ray console",
      "Create a user in the target unified account and generate access and secret keys",
      "Configure the X-Ray daemon to use access and secret keys",
      "Configure the X-Ray daemon to use an IAM instance role",
    ],
    correctAnswerIndex: null,
    correctAnswerIndices: [0, 4],
    isMultiSelect: true,
    explanation:
      "To achieve cross-account tracing with AWS X-Ray, the X-Ray daemon running in a 'source' account must have permissions to upload data to a 'destination' (unified) account. This is accomplished by having the daemon's IAM instance role assume a cross-account IAM role in the destination account. The daemon is then configured to use this assumed role to publish trace data, providing a centralized view of performance across the entire organization.",
    answerExplanations: [
      "Correct. You must establish a trust relationship where the unified account allows the service roles from sub-accounts to assume a specific role with 'xray:PutTraceSegments' permissions.",
      "Incorrect. There is no simple 'toggle' in the X-Ray console to enable cross-account collection; it requires proper IAM role configuration and daemon setup.",
      "Incorrect. While technically possible, using long-lived IAM user credentials (access keys) is a security anti-pattern. IAM roles with temporary security credentials are the best practice.",
      "Incorrect. This follows the insecure path of using hardcoded credentials rather than the more secure and manageable IAM role-based approach.",
      "Correct. The X-Ray daemon running on EC2 should use the IAM instance role to get its base permissions, which then allows it to assume the cross-account role for centralized reporting.",
    ],
  },
  {
    id: "S11q23",
    domain: "Troubleshooting and Optimization",
    text: "The customer feedback functionality for a company's flagship web application is handled via an Amazon API Gateway based REST API that invokes an AWS Lambda function for further processing. Although the performance of the function is satisfactory, the development team has been tasked to optimize the startup time of the Lambda function to further improve the customer experience.\n\nHow will you optimize the Lambda function for faster initialization?",
    answers: [
      "Configure reserved concurrency to guarantee the maximum number of concurrent instances of the Lambda function",
      "Configure an interface VPC endpoint powered by AWS PrivateLink to access the Amazon API Gateway REST API with milliseconds latency",
      "Enable API caching in Amazon API Gateway to cache AWS Lambda function response",
      "Configure provisioned concurrency for the Lambda function to respond immediately to the function's invocations",
    ],
    correctAnswerIndex: 3,
    correctAnswerIndices: [3],
    isMultiSelect: false,
    explanation:
      "To eliminate 'cold starts' and optimize initialization time, AWS provides Provisioned Concurrency. This feature keeps a specified number of execution environments initialized and ready to respond immediately to incoming requests. Unlike standard on-demand scaling, where the runtime and code are loaded only after a request arrives, Provisioned Concurrency performs these steps in advance, ensuring consistent low latency for every invocation.",
    answerExplanations: [
      "Incorrect. Reserved concurrency acts as a quota to ensure a function has enough capacity or to limit its impact on other functions; it does not keep environments warm or reduce startup latency.",
      "Incorrect. VPC endpoints improve network security and reduce public internet traversal, but they do not affect the internal initialization process of the Lambda execution environment.",
      "Incorrect. API Gateway caching improves performance for repeated identical requests by avoiding the Lambda invocation entirely, but it does nothing to fix the startup latency for the requests that *do* need to reach the function.",
      "Correct. Provisioned Concurrency prepares execution environments in advance, running the initialization code before any requests arrive, thus eliminating cold start delays for users.",
    ],
  },
  {
    id: "S11q24",
    domain: "Troubleshooting and Optimization",
    text: "A company's e-commerce application becomes slow when traffic spikes. The application has a three-tier architecture (web, application and database tier) that uses synchronous transactions. The development team at the company has identified certain bottlenecks in the application tier and it is looking for a long term solution to improve the application's performance.\n\nAs a developer associate, which of the following solutions would you suggest to meet the required application response times while accounting for any traffic spikes?",
    answers: [
      "Leverage horizontal scaling for the web and application tiers by using Auto Scaling groups and Application Load Balancer",
      "Leverage SQS with asynchronous AWS Lambda calls to decouple the application and data tiers",
      "Leverage vertical scaling for the application instance by provisioning a larger Amazon EC2 instance size",
      "Leverage horizontal scaling for the application's persistence layer by adding Oracle RAC on AWS",
    ],
    correctAnswerIndex: 0,
    correctAnswerIndices: [0],
    isMultiSelect: false,
    explanation:
      "Horizontal scaling is the best long-term solution for handling unpredictable traffic spikes in a three-tier architecture. By using an Application Load Balancer (ALB) combined with Auto Scaling Groups (ASG), the system can automatically add more EC2 instances when demand increases and remove them when it decreases. This approach provides better fault tolerance and availability compared to vertical scaling and maintains the existing synchronous communication pattern between tiers.",
    answerExplanations: [
      "Correct. Horizontal scaling via ALB and ASG is the standard cloud-native approach to handle traffic spikes. It allows the web and application tiers to grow or shrink dynamically, ensuring performance during peaks and cost-efficiency during lulls.",
      "Incorrect. While SQS and Lambda are excellent for decoupling, the application specifically uses 'synchronous transactions.' Moving to an asynchronous model would require significant code refactoring and architectural changes, whereas horizontal scaling addresses the bottleneck with infrastructure configuration.",
      "Incorrect. Vertical scaling (increasing instance size) has a hard upper limit, results in single points of failure, and usually requires downtime to change instance types. It is considered a temporary 'band-aid' rather than a long-term scalable solution.",
      "Incorrect. Oracle RAC is a high-availability solution for databases, but the bottleneck was identified in the 'application tier,' not the persistence layer. Furthermore, migrating a database to RAC is a complex solution for a simple application-tier performance issue.",
    ],
  },
  {
    id: "S11q25",
    domain: "Security",
    text: "A developer created an online shopping application that runs on EC2 instances behind load balancers. The same web application version is hosted on several EC2 instances and the instances run in an Auto Scaling group. The application uses STS to request credentials but after an hour your application stops working.\n\nWhat is the most likely cause of this issue?",
    answers: [
      "The IAM service is experiencing downtime once an hour",
      "Your IAM policy is wrong",
      "Your application needs to renew the credentials after 1 hour when they expire",
      "A lambda function revokes your access every hour",
    ],
    correctAnswerIndex: 2,
    correctAnswerIndices: [2],
    isMultiSelect: false,
    explanation:
      "AWS Security Token Service (STS) provides temporary security credentials. These credentials have a defined expiration period. For many STS APIs, such as `GetSessionToken`, the default expiration is 1 hour (3,600 seconds). If an application caches these credentials and does not implement a refresh logic to request new ones before they expire, all AWS service calls will fail with an 'ExpiredToken' error after that duration.",
    answerExplanations: [
      "Incorrect. IAM and STS are highly available global services. Systematic downtime occurring exactly every hour is not a characteristic of AWS service health.",
      "Incorrect. If the IAM policy were incorrect, the application would fail immediately upon the first request, not specifically after one hour of successful operation.",
      "Correct. STS credentials are temporary by design. The application must be coded to handle credential expiration by requesting fresh temporary credentials from STS.",
      "Incorrect. While it's technically possible to automate permission revocation, it is not a standard behavior or a 'likely cause' compared to the built-in expiration of temporary tokens.",
    ],
  },
  {
    id: "S11q26",
    domain: "Security",
    text: "An e-commerce company has deployed its application on AWS Elastic Beanstalk. The Auto Scaling group associated with the Beanstalk environment has three Amazon EC2 instances. When the number of instances falls below two, it severely impacts the performance of the web application. The company currently uses the default all-at-once deployment policy and is looking for an effective strategy for future deployments.\n\nWhich of the following represents the most cost-effective deployment strategy for the company?",
    answers: [
      "Opt for traffic-splitting deployment strategy with traffic split parameter set to 50% of the total traffic",
      "Opt for rolling with additional batch deployment strategy. Set the batch size parameter to 1",
      "Opt for rolling deployment strategy. Set the batch size to 2",
      "Configure an Elastic Load Balancer to front the Auto Scaling Group and choose two different Availability Zones (AZs) for deployment",
    ],
    correctAnswerIndex: 1,
    correctAnswerIndices: [1],
    isMultiSelect: false,
    explanation:
      "To maintain full capacity (at least 3 instances) during a deployment while keeping costs low, the 'Rolling with additional batch' strategy is ideal. This strategy first launches a new 'batch' of instances (in this case, 1 instance) with the new version. Once healthy, it proceeds to update the existing instances in batches of 1. Because the extra instance was added first, the total count never drops below the required 3 instances. This is more cost-effective than 'Immutable' or 'Traffic Splitting' because it only adds a small number of temporary instances rather than doubling the fleet.",
    answerExplanations: [
      "Incorrect. Traffic-splitting is a 'Canary' style deployment that launches a full second set of instances (doubling the count). While safe, it is significantly more expensive than adding just a single additional batch.",
      "Correct. By setting the batch size to 1 with an additional batch, Elastic Beanstalk will launch 1 new instance (total = 4), then update/replace the old ones. The environment always has at least 3 healthy instances available to serve traffic.",
      "Incorrect. A standard rolling deployment (without an additional batch) takes instances out of service to update them. If the batch size is 2, the capacity would drop to 1 instance (3 - 2 = 1), which violates the requirement that capacity must not fall below 2.",
      "Incorrect. While multi-AZ deployment is a best practice for high availability, it is not a 'deployment strategy' that solves the problem of maintaining instance capacity during a code update.",
    ],
  },
  {
    id: "S11q27",
    domain: "Development with AWS Services",
    text: "Your Lambda function must use the Node.js drivers to connect to your RDS PostgreSQL database in your VPC.\n\nHow do you bundle your Lambda function to add the dependencies?",
    answers: [
      "Zip the function and the dependencies separately and upload them in AWS Lambda as two parts",
      "Zip the function as-is with a package.json file so that AWS Lambda can resolve the dependencies for you",
      "Upload the code through the AWS console and upload the dependencies as a zip",
      "Put the function and the dependencies in one folder and zip them together",
    ],
    correctAnswerIndex: 3,
    correctAnswerIndices: [3],
    isMultiSelect: false,
    explanation:
      "To include external libraries (like the `pg` driver for PostgreSQL) in a Node.js Lambda function, you must create a deployment package. This is a .zip file that contains your function code along with the `node_modules` folder containing your dependencies. Unlike some other platforms, AWS Lambda does not automatically run `npm install` for you based on a `package.json` file; all required files must be present in the uploaded archive.",
    answerExplanations: [
      "Incorrect. Lambda functions are uploaded as a single deployment package. You cannot split the main code and its direct dependencies into two separate upload parts for the same function version.",
      "Incorrect. AWS Lambda does not resolve or install dependencies during deployment. If you only upload the `package.json` without the `node_modules` folder, the function will fail with a 'module not found' error when it tries to require the database driver.",
      "Incorrect. The AWS console allows you to edit code or upload a zip file, but it does not support a separate 'dependency upload' field for a single function code block.",
      "Correct. This is the standard procedure: run `npm install` locally to generate the `node_modules` folder, then zip that folder together with your `.js` files and upload the resulting archive.",
    ],
  },
  {
    id: "S11q28",
    domain: "Development with AWS Services",
    text: "You are looking to invoke an AWS Lambda function every hour (similar to a cron job) in a serverless way.\n\nWhich event source should you use for your AWS Lambda function?",
    answers: ["EventBridge", "Amazon S3", "SQS", "Kinesis"],
    correctAnswerIndex: 0,
    correctAnswerIndices: [0],
    isMultiSelect: false,
    explanation:
      "To run a Lambda function on a schedule, you use Amazon EventBridge (formerly CloudWatch Events). EventBridge allows you to create a rule that triggers an action (like invoking a Lambda function) based on a schedule expression. You can define these schedules using 'rate' expressions (e.g., `rate(1 hour)`) or 'cron' expressions (e.g., `cron(0 * * * ? *)`) for more complex timing requirements.",
    answerExplanations: [
      "Correct. EventBridge Scheduler or EventBridge Rules are the native AWS services for triggering serverless functions on a recurring time basis.",
      "Incorrect. Amazon S3 triggers Lambda functions based on data events, such as when a file is uploaded or deleted, not based on time.",
      "Incorrect. SQS triggers Lambda when messages are placed in a queue. While you could delay messages, it is not a mechanism for recurring cron-like scheduling.",
      "Incorrect. Kinesis is used for real-time data streaming; it triggers Lambda to process records as they arrive in the stream, not at specific time intervals.",
    ],
  },
  {
    id: "S11q29",
    domain: "Development with AWS Services",
    text: "A financial services company uses Amazon S3 to store transformed and anonymized customer data that is generated by a daily batch job. The development team has been tasked to build a solution that analyzes the output of the daily job for any sensitive financial information about the company's customers.\n\nAs an AWS Certified Developer Associate, which of the following options would you recommend to address this use case MOST efficiently?",
    answers: [
      "Leverage Macie to analyze the output of the daily batch job and look for any sensitive data findings of type SensitiveData:S3Object/CustomIdentifier",
      "Leverage Macie to analyze the output of the daily batch job and look for any sensitive data findings of type SensitiveData:S3Object/Financial",
      "Leverage Macie to analyze the output of the daily batch job and look for any sensitive data findings of type SensitiveData:S3Object/Personal",
      "Configure a S3 event notification for every object upload that triggers a Lambda function based Python script to detect sensitive customer information",
    ],
    correctAnswerIndex: 1,
    correctAnswerIndices: [1],
    isMultiSelect: false,
    explanation:
      "Amazon Macie is a fully managed data security and data privacy service that uses machine learning and pattern matching to discover and protect sensitive data in Amazon S3. For a financial services company, the most efficient approach is to use Macie's built-in managed identifiers. Specifically, the 'Financial' finding type is designed to detect data like credit card numbers, bank account numbers, and other fiscal identifiers. Using a managed service is significantly more efficient than building and maintaining custom detection logic.",
    answerExplanations: [
      "Incorrect. CustomIdentifiers are used for proprietary data formats unique to your business (e.g., specific employee ID formats). Since the requirement mentions 'sensitive financial information,' the built-in Financial managed identifiers are more appropriate.",
      "Correct. This finding type specifically targets financial data (credit cards, bank details, etc.), which perfectly aligns with the company's requirement to monitor for sensitive financial information.",
      "Incorrect. While 'Personal' findings cover PII like home addresses or passports, the prompt specifically highlights 'financial information,' making the Financial finding type the more precise match.",
      "Incorrect. While possible, writing a custom Python script to identify complex sensitive data patterns is highly inefficient. It requires significant development effort, regular maintenance to handle new data formats, and lacks the sophisticated machine learning capabilities provided natively by Macie.",
    ],
  },
  {
    id: "S11q30",
    domain: "Troubleshooting and Optimization",
    text: "Applications running on EC2 instances process messages from an SQS queue but sometimes they experience errors due to messages not being processed.\n\nTo isolate the messages, which option will help with further debugging?",
    answers: [
      "Use DeleteMessage",
      "Reduce the VisibilityTimeout",
      "Implement a Dead Letter Queue",
      "Increase the VisibilityTimeout",
    ],
    correctAnswerIndex: 2,
    correctAnswerIndices: [2],
    isMultiSelect: false,
    explanation:
      "A Dead Letter Queue (DLQ) is a secondary SQS queue used to sideline messages that fail to be processed successfully after a certain number of attempts (defined by the Redrive Policy). By moving 'poison pill' messages—those that cause application errors—to a DLQ, you prevent them from repeatedly failing and clogging the main queue. This isolation allows developers to inspect the message payload and metadata to debug why the processing failed without affecting the rest of the system.",
    answerExplanations: [
      "Incorrect. Using `DeleteMessage` simply removes the message from the queue. If you delete a problematic message before analyzing it, you lose the data needed for debugging.",
      "Incorrect. Reducing the visibility timeout makes the message reappear in the queue faster for other consumers. This would likely cause the error to occur more frequently and could lead to a loop of failures.",
      "Correct. DLQs are specifically designed to isolate failed messages for troubleshooting and to improve the overall resilience of the message processing system.",
      "Incorrect. While increasing the visibility timeout gives a consumer more time to process a single message, it does not help isolate messages that are inherently unprocessable or cause application crashes.",
    ],
  },
  {
    id: "S11q31",
    domain: "Security",
    text: "A business-critical mobile application uses Amazon Cognito user pools with multi-factor authentication (MFA) enabled for all its users. The application manages confidential data about the company's sales forecasts and product launches. Considering the highly critical nature of the application, the company wants to track every user login activity via a notification sent as an email to the security team.\n\nWhich of the following would you recommend as the MOST optimal way of implementing this requirement within a short period?",
    answers: [
      "Create an AWS Lambda function that uses Amazon Simple Email Service to send an email notification to the concerned security team. Configure this function as Amazon Cognito post-authentication Lambda trigger",
      "Configure an AWS Lambda function as a trigger to Amazon Cognito identity pools authenticated API operations. Create the Lambda function to utilize the Amazon Simple Email Service to send an email notification to the concerned security team",
      "Create an AWS Lambda function that uses Amazon Simple Email Service to send an email notification to the concerned security team. Configure this function as Amazon Cognito pre-authentication Lambda trigger",
      "Configure Amazon Cognito user pools authenticated API operations and MFA API operations to send all login data to Amazon Kinesis Data Streams. Configure an AWS Lambda function to analyze these streams and trigger an SNS notification to the security team based on user access",
    ],
    correctAnswerIndex: 0,
    correctAnswerIndices: [0],
    isMultiSelect: false,
    explanation:
      "Amazon Cognito User Pools provide several 'hooks' called Lambda triggers that allow you to inject custom logic into the authentication flow. To track successful login activity, the Post-authentication trigger is the ideal choice. It is invoked by Cognito immediately after a user is authenticated (including MFA verification). By connecting this trigger to a Lambda function that sends an email via Amazon SES, the security team receives an alert only after a verified successful login has occurred.",
    answerExplanations: [
      "Correct. The Post-authentication trigger is specifically designed to run logic after a successful login. It ensures the email is sent only when the user has successfully provided both their password and MFA token.",
      "Incorrect. Identity Pools are used for authorization (granting AWS credentials) rather than the initial authentication of a user's identity. Furthermore, they do not support the same native event triggers as User Pools for tracking login events.",
      "Incorrect. The Pre-authentication trigger runs *before* the user's credentials or MFA are even verified. Sending an email at this stage would notify the team of every login attempt, including failed ones or unauthorized attempts, rather than actual successful logins.",
      "Incorrect. This is a highly complex architectural change that would take a significant amount of time to implement. It is not the 'most optimal' way to satisfy a requirement within a short period when a native trigger exists.",
    ],
  },
  {
    id: "S11q32",
    domain: "Development with AWS Services",
    text: "You are running a public DNS service on an EC2 instance where the DNS name is pointing to the IP address of the instance. You wish to upgrade your DNS service but would like to do it without any downtime.\n\nWhich of the following options will help you accomplish this?",
    answers: [
      "Elastic IP",
      "Create a Load Balancer and an auto scaling group",
      "Use Route 53",
      "Provide a static private IP",
    ],
    correctAnswerIndex: 0,
    correctAnswerIndices: [0],
    isMultiSelect: false,
    explanation:
      "When you run a custom service on EC2 that is identified by a specific IP address (common for DNS servers), an Elastic IP (EIP) is the best way to handle upgrades without downtime. An Elastic IP is a static, public IPv4 address that can be rapidly remapped from one EC2 instance to another within the same region. To upgrade, you can launch a new instance with the updated DNS software and then reassociate the EIP from the old instance to the new one. This ensures that clients pointed at that IP address see no interruption in service.",
    answerExplanations: [
      "Correct. An Elastic IP allows you to mask the failure or upgrade of an instance by remapping the address to another instance in your account.",
      "Incorrect. While Load Balancers provide high availability, they are typically accessed via a DNS name (CNAME/Alias) rather than a static public IP. If your clients are specifically pointing to an IP address, a standard ALB/NLB won't solve this without further complexity.",
      "Incorrect. The scenario specifies that you are *running* a DNS service on EC2. Switching to Route 53 would mean migrating your service to an AWS managed DNS, which is a different architectural choice than upgrading your existing EC2-based service.",
      "Incorrect. A private IP is only reachable within a VPC or connected network. Since this is a public DNS service, clients on the internet must be able to reach it via a public IP.",
    ],
  },
  {
    id: "S11q33",
    domain: "Deployment",
    text: "You've just deployed an AWS Lambda function. The lambda function will be invoked via the API Gateway. The API Gateway will need to control access to it.\n\nWhich of the following mechanisms is not supported for API Gateway?",
    answers: [
      "Cognito User Pools",
      "IAM permissions with sigv4",
      "STS",
      "Lambda Authorizer",
    ],
    correctAnswerIndex: 2,
    correctAnswerIndices: [2],
    isMultiSelect: false,
    explanation:
      "Amazon API Gateway supports several authorization mechanisms to secure your APIs. These include IAM (using Signature Version 4), Amazon Cognito User Pools, and Lambda Authorizers. While the Security Token Service (STS) is used to generate temporary credentials for IAM, it is not an integrated 'authorizer' type that you configure within API Gateway settings. You would configure an IAM authorizer, and the caller might use STS to obtain the credentials needed to satisfy that IAM requirement.",
    answerExplanations: [
      "Incorrect. Cognito User Pools are a supported native authorizer for REST APIs, allowing you to use JWT tokens for authentication.",
      "Incorrect. IAM authorization with SigV4 is a standard way to secure APIs, requiring the caller to sign the request with AWS credentials.",
      "Correct. STS is a service used to get temporary security credentials; it is not a direct authorization mechanism configurable on an API Gateway resource or method.",
      "Incorrect. Lambda Authorizers (formerly Custom Authorizers) allow you to use a Lambda function to provide a custom authorization scheme (e.g., OAuth, SAML, or custom headers).",
    ],
  },
  {
    id: "S11q34",
    domain: "Security",
    text: "A company has a new media application that utilizes an Amazon CloudFront distribution that accesses the S3 bucket by using an origin access identity (OAI). The S3 bucket has an explicit access denial for all other users. A developer wants to allow access to the login page for unauthenticated users while ensuring the security of all private content that has restricted viewer access.\n\nWhich of the following will you recommend?",
    answers: [
      "Configure a second cache behavior to the distribution having the same origin as the default cache behavior and have the path pattern for the second cache behavior as the path of the login page with viewer access as unrestricted. Keep the default cache behavior’s settings unchanged",
      "Configure a second origin as the failover origin for the default behavior of the original distribution and have the path pattern for the second origin as the path of the login page with viewer access as unrestricted. Keep the behavior for the primary origin unchanged",
      "Configure a second cache behavior to the distribution having the same origin as the default cache behavior and have the path pattern for the second cache behavior as * with viewer access as restricted. Modify the default cache behavior’s path pattern to the path of the login page and have the viewer access as unrestricted",
      "Configure a new distribution having the same origin as the original distribution and set the path pattern for the default cache behavior of the new distribution as the path of the login page with viewer access as unrestricted. Keep the default cache behavior of the original distribution unchanged",
    ],
    correctAnswerIndex: 0,
    correctAnswerIndices: [0],
    isMultiSelect: false,
    explanation:
      "In Amazon CloudFront, you can define multiple cache behaviors to apply different settings to different parts of your website based on URL path patterns. Since the default cache behavior (path pattern `*`) is already set to restricted access, you should add a more specific cache behavior for the login page (e.g., `/login.html`). Specific path patterns take precedence over the default behavior. By leaving the default behavior restricted and making the specific login path unrestricted, you allow public access to the login page while keeping the rest of the site's content secure.",
    answerExplanations: [
      "Correct. Adding a specific cache behavior for the login page allows you to override the restricted access settings for just that path, while the catch-all default behavior (`*`) remains restricted.",
      "Incorrect. Origin failover is used for high availability (switching origins if one fails), not for managing different access permissions based on path patterns.",
      "Incorrect. The path pattern for the default cache behavior is always `*` and cannot be changed. You must use additional cache behaviors to create specific rules for specific paths.",
      "Incorrect. While creating a separate distribution would work, it is not the most efficient or recommended way to manage different access rules for the same application. Using multiple cache behaviors within a single distribution is the standard best practice.",
    ],
  },
  {
    id: "S11q35",
    domain: "Deployment",
    text: "An IT company leverages CodePipeline to automate its release pipelines. The development team wants to write a Lambda function that will send notifications for state changes within the pipeline.\n\nAs a Developer Associate, which steps would you suggest to associate the Lambda function with the event source?",
    answers: [
      "Use the Lambda console to configure a trigger that invokes the Lambda function with CodePipeline as the event source",
      "Set up an Amazon CloudWatch alarm that monitors status changes in Code Pipeline and triggers the Lambda function",
      "Set up an Amazon EventBridge rule that uses CodePipeline as an event source with the target as the Lambda function",
      "Use the CodePipeline console to set up a trigger for the Lambda function",
    ],
    correctAnswerIndex: 2,
    correctAnswerIndices: [2],
    isMultiSelect: false,
    explanation:
      "To react to state changes in AWS services like CodePipeline, the standard architecture is to use Amazon EventBridge (formerly CloudWatch Events). CodePipeline emits events when a pipeline, stage, or action changes state (e.g., STARTED, SUCCEEDED, FAILED). By creating an EventBridge rule, you can match these specific events and define the Lambda function as the target to be invoked automatically whenever the rule is triggered.",
    answerExplanations: [
      "Incorrect. While the Lambda console allows you to add triggers, CodePipeline is not a direct push-event source for Lambda. It requires EventBridge to act as the intermediary to catch the state change and route it to Lambda.",
      "Incorrect. CloudWatch Alarms are used to monitor metrics (like CPU usage or error counts) over time. They are not designed to capture discrete state-change events from a developer tool like CodePipeline.",
      "Correct. EventBridge is the designated service for handling system events in near real-time. It allows you to filter for CodePipeline state changes and trigger various targets, including Lambda, SNS, or SQS.",
      "Incorrect. CodePipeline does not have a native 'Triggers' section within its console to directly invoke Lambda functions for state notifications; this orchestration is handled via EventBridge.",
    ],
  },
  {
    id: "S11q36",
    domain: "Deployment",
    text: "Your company is shifting towards Elastic Container Service (ECS) to deploy applications. The process should be automated using the AWS CLI to create a service where at least ten instances of a task definition are kept running under the default cluster.\n\nWhich of the following commands should be executed?",
    answers: [
      "aws ecs run-task --cluster default --task-definition ecs-demo",
      "aws ecr create-service --service-name ecs-simple-service --task-definition ecs-demo --desired-count 10",
      "docker-compose create ecs-simple-service",
      "aws ecs create-service --service-name ecs-simple-service --task-definition ecs-demo --desired-count 10",
    ],
    correctAnswerIndex: 3,
    correctAnswerIndices: [3],
    isMultiSelect: false,
    explanation:
      "To maintain a specific number of tasks running continuously, you must create an ECS Service. The `aws ecs create-service` command allows you to specify the `desired-count`, which instructs the ECS scheduler to launch and maintain the requested number of tasks. If a task fails, the service scheduler will automatically replace it to maintain the count of 10.",
    answerExplanations: [
      "Incorrect. The `run-task` command is used to start one or more tasks manually. It does not create a 'service' and will not automatically restart or maintain the tasks if they fail.",
      "Incorrect. This command incorrectly references `ecr` (Elastic Container Registry), which is used for storing images, not for running services. The correct service namespace is `ecs`.",
      "Incorrect. `docker-compose` is a tool for defining and running multi-container Docker applications locally. While there is a CLI integration for ECS, this is not a standard AWS CLI command for creating an ECS service.",
      "Correct. This is the standard AWS CLI command to create an ECS service with the required task definition and desired capacity of 10 tasks.",
    ],
  },
  {
    id: "S11q37",
    domain: "Troubleshooting and Optimization",
    text: "Your company is new to cloud computing and would like to host a static HTML5 website on the cloud and be able to access it via domain www.mycompany.com. You have created a bucket in Amazon Simple Storage Service (S3), enabled website hosting, and set the index.html as the default page. Finally, you create an Alias record in Amazon Route 53 that points to the S3 website endpoint of your S3 bucket.\n\nWhen you test the domain www.mycompany.com you get the following error: 'HTTP response code 403 (Access Denied)'. What can you do to resolve this error?",
    answers: [
      "Create a bucket policy",
      "Enable CORS",
      "Create an IAM role",
      "Enable Encryption",
    ],
    correctAnswerIndex: 0,
    correctAnswerIndices: [0],
    isMultiSelect: false,
    explanation:
      "By default, all S3 buckets and their objects are private. Enabling 'Static Website Hosting' on a bucket makes the endpoint available, but it does not automatically grant the public permission to read the files. An 'HTTP 403 Forbidden' error in this context specifically indicates that the requester does not have the `s3:GetObject` permission. To fix this, you must apply a public bucket policy that explicitly allows anonymous 'Read' access to all objects within the bucket.",
    answerExplanations: [
      "Correct. A bucket policy is required to grant the 'public-read' permission to anonymous users so they can view your website content.",
      "Incorrect. Cross-Origin Resource Sharing (CORS) is used when a web application in one domain needs to access resources in another domain. It does not resolve basic permission issues for viewing the primary website files.",
      "Incorrect. IAM roles are used to grant permissions to AWS services or authenticated users. Since website visitors are anonymous public users, an IAM role cannot be assigned to them.",
      "Incorrect. Encryption (SSE) handles how data is stored at rest. It does not control public accessibility and, in some misconfigurations, could actually cause further access issues rather than solving a 403 error.",
    ],
  },
  {
    id: "S11q38",
    domain: "Deployment",
    text: "A media analytics company has built a streaming application on Lambda using Serverless Application Model (SAM).\n\nAs a Developer Associate, which of the following would you identify as the correct order of execution to successfully deploy the application?",
    answers: [
      "Develop the SAM template locally => upload the template to Lambda => deploy your application to the cloud",
      "Develop the SAM template locally => upload the template to CodeCommit => deploy your application to CodeDeploy",
      "Develop the SAM template locally => upload the template to S3 => deploy your application to the cloud",
      "Develop the SAM template locally => deploy the template to S3 => use your application in the cloud",
    ],
    correctAnswerIndex: 2,
    correctAnswerIndices: [2],
    isMultiSelect: false,
    explanation:
      "The AWS Serverless Application Model (SAM) deployment workflow follows a specific lifecycle. First, you develop your template and code locally. Second, you package the application, which zips the artifacts and uploads them to an Amazon S3 bucket (using `sam package`). This step transforms the local paths in your template into S3 URLs. Finally, you deploy the application (using `sam deploy`), which uses AWS CloudFormation to create or update the resources in the cloud based on the transformed template.",
    answerExplanations: [
      "Incorrect. You do not upload templates directly to Lambda; Lambda is a compute service that runs the code defined in your deployment package, which is typically stored in S3 during the deployment process.",
      "Incorrect. While you can store your code in CodeCommit, SAM uses CloudFormation (not directly CodeDeploy) as its primary deployment engine. The packaging step involving S3 is a mandatory part of the SAM CLI workflow.",
      "Correct. This represents the standard 'Package and Deploy' workflow where S3 acts as the staging area for your code and transformed templates before CloudFormation executes the deployment.",
      "Incorrect. While this looks similar to the correct answer, 'deploy the template to S3' is slightly inaccurate terminology, and 'use your application' describes the end result rather than the technical deployment step to the AWS Cloud environment.",
    ],
  },
  {
    id: "S11q39",
    domain: "Deployment",
    text: "You would like to run the X-Ray daemon for your Docker containers deployed using AWS Fargate.\n\nWhat do you need to do to ensure the setup will work? (Select two)",
    answers: [
      "Provide the correct IAM task role to the X-Ray container",
      "Deploy the X-Ray daemon agent as a sidecar container",
      "Deploy the X-Ray daemon agent as a daemon set on ECS",
      "Provide the correct IAM instance role to the EC2 instance",
      "Deploy the X-Ray daemon agent as a process on your EC2 instance",
    ],
    correctAnswerIndex: null,
    correctAnswerIndices: [0, 1],
    isMultiSelect: true,
    explanation:
      "When deploying applications on AWS Fargate, you do not have access to the underlying EC2 instances. Therefore, traditional methods like running the X-Ray daemon as a background process on a host or using an ECS 'Daemon' scheduling strategy (which runs one task per host) are not applicable. Instead, you must use the 'Sidecar' pattern, where the X-Ray daemon runs as an additional container within the same Fargate Task definition. Additionally, because the daemon needs to upload trace data to the AWS X-Ray service, the Task must be assigned an IAM Task Role with the necessary permissions (`xray:PutTraceSegments` and `xray:PutTelemetryRecords`).",
    answerExplanations: [
      "Correct. The IAM Task Role provides the permissions required by the application and the X-Ray daemon container at runtime to interact with AWS services.",
      "Correct. In Fargate, the sidecar pattern is the standard way to run the X-Ray daemon alongside your application container in the same task.",
      "Incorrect. The 'Daemon' service scheduler is only available for ECS clusters with the EC2 launch type; it is not supported in Fargate.",
      "Incorrect. Fargate is serverless, meaning you do not manage or have access to EC2 instances. IAM Instance Roles are only relevant for the EC2 launch type.",
      "Incorrect. This is not possible with Fargate as you do not manage the host operating system or the underlying EC2 instances.",
    ],
  },
  {
    id: "S11q40",
    domain: "Security",
    text: "You are running a web application where users can author blogs and share them with their followers. Most of the workflow is read based, but when a blog is updated, you would like to ensure that the latest data is served to the users (no stale data). The Developer has already suggested using ElastiCache to cope with the read load but has asked you to implement a caching strategy that complies with the requirements of the site.\n\nWhich strategy would you recommend?",
    answers: [
      "Use a Lazy Loading strategy with TTL",
      "Use a Lazy Loading strategy without TTL",
      "Use a Write Through strategy",
      "Use DAX",
    ],
    correctAnswerIndex: 2,
    correctAnswerIndices: [2],
    isMultiSelect: false,
    explanation:
      "A Write-Through caching strategy ensures that data in the cache is never stale. Whenever the application performs a write operation to the database, it simultaneously updates the cache. This guarantees that the most recent data is always available in the cache for subsequent reads. While this adds some latency to write operations and may lead to 'cache churn' (storing data that might not be read immediately), it perfectly satisfies the requirement of preventing stale data delivery.",
    answerExplanations: [
      "Incorrect. Lazy Loading (also known as Cache-Aside) only updates the cache when a cache miss occurs. If a blog is updated in the database, the cache will still hold the old version until the TTL expires, leading to the delivery of stale data.",
      "Incorrect. Without a TTL, the stale data in the cache would persist indefinitely until the cache is manually evicted or the memory is full, which fails the requirement to serve the latest data.",
      "Correct. The Write-Through strategy synchronizes the cache with the database during every write/update, ensuring that reads from the cache always return the most current version of the data.",
      "Incorrect. Amazon DynamoDB Accelerator (DAX) is a specialized in-memory cache exclusively for DynamoDB. Since the question specifies implementing a strategy for ElastiCache, DAX is not the appropriate choice.",
    ],
  },
  {
    id: "S11q41",
    domain: "Troubleshooting and Optimization",
    text: "You have created a DynamoDB table to support your application and provisioned RCU and WCU to it so that your application has been running for over a year now without any throttling issues. Your application now requires a second type of query over your table and as such, you have decided to create an LSI and a GSI on a new table to support that use case. One month after having implemented such indexes, it seems your table is experiencing throttling.\n\nUpon looking at the table's metrics, it seems the RCU and WCU provisioned are still sufficient. What's happening?",
    answers: [
      "The GSI is throttling so you need to provision more RCU and WCU to the GSI",
      "The LSI is throttling so you need to provision more RCU and WCU to the LSI",
      "Adding both an LSI and a GSI to a table is not recommended by AWS best practices as this is a known cause for creating throttles",
      "Metrics are lagging in your CloudWatch dashboard and you should see the RCU and WCU peaking for the main table in a few minutes",
    ],
    correctAnswerIndex: 0,
    correctAnswerIndices: [0],
    isMultiSelect: false,
    explanation:
      "In DynamoDB, Global Secondary Indexes (GSIs) have their own provisioned throughput (RCU and WCU) that is independent of the base table. A critical behavior of GSIs is that if a write to the base table triggers an update to a GSI, and that GSI is throttled due to insufficient WCU, the write operation on the base table will also be throttled. Local Secondary Indexes (LSIs), on the other hand, share the provisioned throughput of the base table. Therefore, if base table metrics look fine but you are still experiencing throttling after adding indexes, the GSI's independent capacity is the likely culprit.",
    answerExplanations: [
      "Correct. GSIs must be provisioned with enough capacity to handle the writes propagated from the base table. If the GSI throughput is exceeded, it back-pressures the base table, causing general throttling.",
      "Incorrect. LSIs do not have their own provisioned throughput; they consume the RCU and WCU of the base table. You cannot 'provision more' to an LSI specifically.",
      "Incorrect. AWS allows and supports using both LSIs and GSIs on the same table. The issue isn't the combination, but the capacity management of the GSI.",
      "Incorrect. While CloudWatch has slight latency, a month of consistent behavior followed by throttling after a GSI implementation strongly points to a configuration issue rather than a dashboard lag.",
    ],
  },
  {
    id: "S11q42",
    domain: "Security",
    text: "You would like your Elastic Beanstalk environment to expose an HTTPS endpoint instead of an HTTP endpoint to get in-flight encryption between your clients and your web servers.\n\nWhat must be done to set up HTTPS on Beanstalk?",
    answers: [
      "Configure Health Checks",
      "Use a separate CloudFormation template to load the SSL certificate onto the Load Balancer",
      "Create a config file in the .ebextensions folder to configure the Load Balancer",
      "Open up the port 80 for the security group",
    ],
    correctAnswerIndex: 2,
    correctAnswerIndices: [2],
    isMultiSelect: false,
    explanation:
      "To enable HTTPS in an Elastic Beanstalk environment, you must configure the environment's Load Balancer to handle SSL/TLS traffic. This is typically done by adding a configuration file in the `.ebextensions` directory. This file (e.g., `https-instance.config`) tells Beanstalk to open port 443 on the Load Balancer, specify the HTTPS protocol, and associate an SSL certificate ARN (from AWS Certificate Manager). Using `.ebextensions` allows you to manage these infrastructure settings as code within your application source bundle.",
    answerExplanations: [
      "Incorrect. While health checks are necessary for the Load Balancer to route traffic to healthy instances, they do not manage SSL certificates or enable HTTPS endpoints.",
      "Incorrect. Elastic Beanstalk manages its own resources. Attempting to modify the Beanstalk-managed Load Balancer via an external CloudFormation template is not the standard or recommended approach; configuration should be done through Beanstalk settings or `.ebextensions`.",
      "Correct. Using `.ebextensions` configuration files is the standard method to customize the environment's Load Balancer to support HTTPS listeners and certificates.",
      "Incorrect. Port 80 is used for unencrypted HTTP traffic. For HTTPS, you must open and configure port 443.",
    ],
  },
  {
    id: "S11q43",
    domain: "Development with AWS Services",
    text: "You are a developer working on AWS Lambda functions that are triggered by Amazon API Gateway and would like to perform testing on a low volume of traffic for new API versions.\n\nWhich of the following features will accomplish this task?",
    answers: [
      "Canary Deployment",
      "Mapping Templates",
      "Stage Variables",
      "Custom Authorizers",
    ],
    correctAnswerIndex: 0,
    correctAnswerIndices: [0],
    isMultiSelect: false,
    explanation:
      "Amazon API Gateway allows you to implement a Canary Release deployment strategy. This feature enables you to direct a small, configurable percentage of traffic (the 'canary') to a new version of your API while the majority of traffic continues to hit the stable production version. This allows for real-world testing and monitoring of the new version's performance and error rates before a full rollout.",
    answerExplanations: [
      "Correct. Canary deployments allow for gradual shifting of traffic to a new version, making it the ideal tool for low-volume production testing.",
      "Incorrect. Mapping templates are used to transform the request/response body and headers between the client and the backend; they do not control traffic distribution between versions.",
      "Incorrect. Stage variables are name-value pairs used as configuration attributes (like environment variables) for an API stage. While they can be used to point to different Lambda aliases, they don't natively provide the weighted traffic splitting required for this testing scenario.",
      "Incorrect. Custom Authorizers (Lambda Authorizers) are used to control access to your API via bearer tokens or request parameters; they are security components, not deployment management tools.",
    ],
  },
  {
    id: "S11q44",
    domain: "Security",
    text: "Your organization has a single Amazon Simple Storage Service (S3) bucket that contains folders labeled with customer names. Several administrators have IAM access to the S3 bucket and versioning is enabled to easily recover from unintended user actions.\n\nWhich of the following statements about versioning is NOT true based on this scenario?",
    answers: [
      "Deleting a file is a recoverable operation",
      "Versioning can be enabled only for a specific folder",
      "Any file that was unversioned before enabling versioning will have the 'null' version",
      "Overwriting a file increases its versions",
    ],
    correctAnswerIndex: 1,
    correctAnswerIndices: [1],
    isMultiSelect: false,
    explanation:
      "In Amazon S3, versioning is a bucket-level configuration. Once enabled, it applies to every object across the entire bucket, regardless of the prefix (folder) structure. You cannot selectively enable or disable versioning for specific folders or sub-directories within a bucket.",
    answerExplanations: [
      "Incorrect (This is true). When you delete an object in a versioned bucket, S3 adds a 'Delete Marker.' You can recover the file by simply deleting that marker to reveal the previous version.",
      "Correct (This is NOT true). Versioning is binary at the bucket level; it is either ON or OFF for the whole bucket. It cannot be scoped to specific folders.",
      "Incorrect (This is true). Objects that existed in the bucket before versioning was enabled are assigned a version ID of 'null'.",
      "Incorrect (This is true). Every time you upload an object with a key that already exists, S3 creates a new version of that object rather than replacing the old data, allowing you to access any previous state.",
    ],
  },
  {
    id: "S11q45",
    domain: "Security",
    text: "An e-commerce application posts its order transactions in bulk to an accounting application for further processing. Due to changes in the compliance rules, all the transactions are being encrypted with AWS Key Management Service (AWS KMS) key before posting to the accounting application. Post this change, the testers have raised tickets regarding the application receiving a ThrottlingException error.\n\nWhat measures should a developer take to fix this issue MOST optimally? (Select two)",
    answers: [
      "Use a bucket-level key for SSE-KMS which will decrease the requested traffic to AWS KMS thereby avoiding the ThrottlingException error",
      "Reduce the rate of requests and consider using the backoff and retry logic",
      "Write queries in Amazon CloudWatch Logs Insights to track your API request usage and submit an AWS Support case to request a quota increase",
      "Send AWS CloudTrail events generated by AWS KMS to Amazon CloudWatch Logs",
      "Use the data key caching feature with the AWS Encryption SDK encryption library",
    ],
    correctAnswerIndex: null,
    correctAnswerIndices: [1, 4],
    isMultiSelect: true,
    explanation:
      "KMS has request quotas (limits) for cryptographic operations. When processing bulk transactions, calling KMS for every single encryption can easily exceed these limits. To optimize, developers should: 1. Implement exponential backoff and retries to gracefully handle transient throttling. 2. Use the AWS Encryption SDK with Data Key Caching. This allows the application to reuse a data key for multiple encryptions for a certain period, significantly reducing the number of direct calls to the KMS API and thus avoiding the ThrottlingException.",
    answerExplanations: [
      "Incorrect. While bucket-level keys exist for S3, the question describes a custom application-to-application posting workflow, not specifically an S3-based storage issue.",
      "Correct. Standard practice for any AWS throttling issue is to implement retry logic with exponential backoff to ensure requests eventually succeed without overwhelming the service.",
      "Incorrect. While you can request quota increases, this is a reactive measure. The 'most optimal' developer solution involves reducing the number of API calls through better architecture (like caching).",
      "Incorrect. Monitoring via CloudTrail and CloudWatch Logs helps identify the problem, but it does not fix the throttling itself.",
      "Correct. Data key caching is the most efficient way to reduce KMS API calls in high-volume encryption scenarios while maintaining security best practices.",
    ],
  },
  {
    id: "S11q46",
    domain: "Development with AWS Services",
    text: "Your AWS account is now growing to 200 users and you would like to provide each of these users a personal space in the S3 bucket 'my_company_space' with the prefix /home/<username>, where they have read/write access.\n\nHow can you do this efficiently?",
    answers: [
      "Create one customer-managed policy per user and attach them to the relevant users",
      "Create inline policies for each user as they are onboarded",
      "Create one customer-managed policy with policy variables and attach it to a group of all users",
      "Create an S3 bucket policy and change it as users are added and removed",
    ],
    correctAnswerIndex: 2,
    correctAnswerIndices: [2],
    isMultiSelect: false,
    explanation:
      "To manage permissions for a large number of users efficiently, AWS IAM provides 'Policy Variables'. Instead of hardcoding a specific username into a policy, you can use `${aws:username}`. When a user makes a request, AWS replaces this variable with the actual friendly name of the user. By attaching one policy using this variable to a group, you grant each user access to their specific folder (e.g., `/home/john` or `/home/jane`) without needing to create or update policies every time a new user is added.",
    answerExplanations: [
      "Incorrect. Managing 200 separate policies is an administrative nightmare and does not scale as the company grows.",
      "Incorrect. Inline policies are maintained within the user identity itself. Creating 200 individual inline policies is inefficient and makes auditing or updating global permissions difficult.",
      "Correct. Using policy variables allows for a single, dynamic policy that covers all users, drastically reducing management overhead.",
      "Incorrect. S3 bucket policies have a size limit (20 KB). Hardcoding 200 users into a single bucket policy would likely exceed this limit and requires manual updates for every team change.",
    ],
  },
  {
    id: "S11q47",
    domain: "Security",
    text: "A security company is requiring all developers to perform server-side encryption with customer-provided encryption keys when performing operations in AWS S3. Developers should write software with C# using the AWS SDK and implement the requirement in the PUT, GET, Head, and Copy operations.\n\nWhich of the following encryption methods meets this requirement?",
    answers: ["SSE-S3", "SSE-KMS", "SSE-C", "Client-Side Encryption"],
    correctAnswerIndex: 2,
    correctAnswerIndices: [2],
    isMultiSelect: false,
    explanation:
      "Server-Side Encryption with Customer-Provided Keys (SSE-C) allows the customer to manage the encryption keys while Amazon S3 manages the encryption and decryption processes. When using SSE-C, the customer must provide the encryption key in every HTTP request (PUT, GET, HEAD, and COPY). S3 does not store the key; it uses the key to encrypt the data as it writes to disk and then discards the key. For subsequent access, the user must provide the exact same key again for S3 to decrypt the object.",
    answerExplanations: [
      "Incorrect. SSE-S3 uses keys managed entirely by Amazon S3. The customer has no control over the key itself.",
      "Incorrect. While SSE-KMS allows for customer-managed keys (CMKs) within the AWS Key Management Service, the keys are still stored and managed by AWS, not provided by the customer in each request.",
      "Correct. SSE-C specifically requires the customer to provide the encryption key as part of the request, meeting the requirement for customer-provided keys while still performing the encryption on the server side.",
      "Incorrect. Client-Side Encryption involves encrypting the data before it is sent to S3. The requirement specified performing server-side encryption, where S3 handles the encryption process.",
    ],
  },
  {
    id: "S11q48",
    domain: "Deployment",
    text: "You are implementing a banking application in which you need to update the Exchanges DynamoDB table and the AccountBalance DynamoDB table at the same time or not at all.\n\nWhich DynamoDB feature should you use?",
    answers: [
      "DynamoDB Streams",
      "DynamoDB TTL",
      "DynamoDB Indexes",
      "DynamoDB Transactions",
    ],
    correctAnswerIndex: 3,
    correctAnswerIndices: [3],
    isMultiSelect: false,
    explanation:
      "To handle 'all-or-nothing' operations across one or more tables, Amazon DynamoDB provides Transactions. DynamoDB transactions provide Atomicity, Consistency, Isolation, and Durability (ACID), ensuring that either all the operations in the transaction succeed, or none of them are applied. This is critical for financial applications where balancing accounts and recording exchanges must stay perfectly synchronized.",
    answerExplanations: [
      "Incorrect. DynamoDB Streams are used to capture changes to items in a table in real-time. While they can trigger downstream processes, they do not provide atomic 'all-or-nothing' write capabilities across multiple tables.",
      "Incorrect. Time to Live (TTL) is a mechanism for automatically deleting items after a certain timestamp. It has no relation to atomic write operations.",
      "Incorrect. Global and Local Secondary Indexes are used to query data using different attributes. They do not manage the atomicity of writes across multiple tables.",
      "Correct. DynamoDB Transactions allow you to group multiple 'Put', 'Update', 'Delete', and 'Check' operations together as a single unit of work that either succeeds or fails as a whole.",
    ],
  },
  {
    id: "S11q49",
    domain: "Troubleshooting and Optimization",
    text: "You are responsible for an application that runs on multiple Amazon EC2 instances. In front of the instances is an Internet-facing load balancer that takes requests from clients over the internet and distributes them to the EC2 instances. A health check is configured to ping the index.html page found in the root directory for the health status. When accessing the website via the internet visitors of the website receive timeout errors.\n\nWhat should be checked first to resolve the issue?",
    answers: [
      "The ALB is warming up",
      "Security Groups",
      "The application is down",
      "IAM Roles",
    ],
    correctAnswerIndex: 1,
    correctAnswerIndices: [1],
    isMultiSelect: false,
    explanation:
      "In AWS, a 'Connection Timeout' is almost always a networking or firewall issue. Security Groups act as virtual firewalls; if the Security Group associated with the Load Balancer does not allow inbound traffic from the internet on the web port (80 or 443), or if the EC2 Security Group does not allow inbound traffic from the Load Balancer, the connection will be dropped silently, resulting in a timeout. While an application being 'down' usually results in a '502 Bad Gateway' or '503 Service Unavailable' error, a timeout indicates the request never reached its destination or the response couldn't get back out.",
    answerExplanations: [
      "Incorrect. While ELBs (Classic) sometimes required pre-warming for huge traffic spikes, modern ALBs scale effectively and 'warming up' would not cause a total connection timeout for general visitors.",
      "Correct. Security Groups are the most common cause of timeouts. You must ensure the ALB security group allows traffic from 0.0.0.0/0 and the EC2 security group allows traffic from the ALB's security group.",
      "Incorrect. If the application is down but the network path is open, the Load Balancer would typically return a 5xx error or a 'Connection Refused' message rather than a timeout.",
      "Incorrect. IAM roles govern permissions for AWS service API calls (like a Lambda writing to S3). They do not control the flow of standard HTTP/HTTPS network traffic to EC2 instances.",
    ],
  },
  {
    id: "S11q50",
    domain: "Security",
    text: "You are deploying Lambda functions that operate on your S3 buckets to read files and extract key metadata. The Lambda functions are managed using SAM.\n\nWhich Policy should you insert in your serverless model template to give buckets read access?",
    answers: [
      "LambdaInvokePolicy",
      "SQSPollerPolicy",
      "S3ReadPolicy",
      "S3CrudPolicy",
    ],
    correctAnswerIndex: 2,
    correctAnswerIndices: [2],
    isMultiSelect: false,
    explanation:
      "AWS SAM (Serverless Application Model) provides 'Policy Templates' which are a set of pre-defined IAM policies that can be easily added to a Lambda function definition. These templates simplify the process of following the principle of least privilege. The `S3ReadPolicy` is specifically designed to provide read-only access to objects within a specified S3 bucket, which is exactly what is required for a metadata extraction task.",
    answerExplanations: [
      "Incorrect. `LambdaInvokePolicy` allows a resource to invoke another Lambda function, which is not relevant for reading S3 objects.",
      "Incorrect. `SQSPollerPolicy` provides the necessary permissions for a Lambda function to poll an SQS queue and retrieve messages.",
      "Correct. `S3ReadPolicy` provides the `GetObject` and `ListBucket` permissions required to read files and metadata from S3.",
      "Incorrect. `S3CrudPolicy` provides Create, Read, Update, and Delete permissions. While it would allow the function to read files, it violates the principle of least privilege by granting unnecessary write and delete permissions.",
    ],
  },
  {
    id: "S11q51",
    domain: "Deployment",
    text: "Your client wants to deploy a service on EC2 instances, and as EC2 instances are added into an ASG, each EC2 instance should be running 3 different Docker Containers simultaneously.\n\nWhat Elastic Beanstalk platform should they choose?",
    answers: [
      "Docker single-container platform",
      "Docker multi-container platform",
      "Third-party platform",
      "Custom platform",
    ],
    correctAnswerIndex: 1,
    correctAnswerIndices: [1],
    isMultiSelect: false,
    explanation:
      "To run multiple Docker containers on a single EC2 instance within an Elastic Beanstalk environment, you must use a platform that supports multi-container deployments. Historically, this was specifically called the 'Multi-container Docker' platform (which uses Amazon ECS as the orchestration engine). In modern Amazon Linux 2 and AL2023 environments, Elastic Beanstalk has unified the Docker platforms, but the architectural requirement remains the same: you select the platform that supports multiple containers (often configured via a `docker-compose.yml` or a `Dockerrun.aws.json v2` file) to ensure that all containers are provisioned together on each instance in the Auto Scaling Group.",
    answerExplanations: [
      "Incorrect. The single-container platform is designed to run exactly one Docker container per instance. It uses a `Dockerfile` or `Dockerrun.aws.json v1` and includes a default Nginx reverse proxy.",
      "Correct. The multi-container platform allows you to define multiple containers in a single environment. Elastic Beanstalk coordinates these using Amazon ECS or Docker Compose to ensure all three containers run on every instance.",
      "Incorrect. There is no 'Third-party platform' in Elastic Beanstalk; this is a distractor option.",
      "Incorrect. While a Custom Platform allows you to build an entire OS and software stack from scratch, it is unnecessary here since Elastic Beanstalk provides a native, managed platform specifically for multi-container Docker use cases.",
    ],
  },
  {
    id: "S11q52",
    domain: "Troubleshooting and Optimization",
    text: "A popular mobile app retrieves data from an AWS DynamoDB table that was provisioned with read-capacity units (RCUs) that are evenly shared across four partitions. One of those partitions is receiving more traffic than the other partitions, causing hot partition issues.\n\nWhat technology will allow you to reduce the read traffic on your AWS DynamoDB table with minimal effort?",
    answers: [
      "DynamoDB Streams",
      "ElastiCache",
      "DynamoDB DAX",
      "More partitions",
    ],
    correctAnswerIndex: 2,
    correctAnswerIndices: [2],
    isMultiSelect: false,
    explanation:
      "Amazon DynamoDB Accelerator (DAX) is an API-compatible in-memory cache specifically designed for DynamoDB. It is considered the 'minimal effort' solution because it does not require developers to manage cache-invalidation logic or rewrite complex application code; you simply point your DynamoDB client to the DAX endpoint. By caching frequently accessed items (especially those causing a 'hot partition'), DAX offloads the read volume from the underlying table, effectively resolving throttling and reducing RCU consumption.",
    answerExplanations: [
      "Incorrect. DynamoDB Streams capture item-level changes (inserts, updates, deletes) in a table. They are used for triggering Lambda functions or replicating data, not for caching or reducing read traffic.",
      "Incorrect. While ElastiCache (Redis or Memcached) can reduce read load, it is not a 'minimal effort' solution in this context. It requires significant application code changes to implement the cache-aside pattern (checking the cache, handling misses, and updating the cache).",
      "Correct. DAX is a managed, seamless cache that requires minimal code changes and is specifically built to handle hot partitions and reduce RCU load on DynamoDB tables.",
      "Incorrect. You cannot manually add more partitions to 'dilute' a hot key. While DynamoDB manages partitions automatically, if a single partition-key is being hit too hard, adding more partitions won't solve the problem if the traffic remains concentrated on that specific key.",
    ],
  },
  {
    id: "S11q53",
    domain: "Deployment",
    text: "You have created a test environment in Elastic Beanstalk and as part of that environment, you have created an RDS database.\n\nHow can you make sure the database can be explored after the environment is destroyed?",
    answers: [
      "Make a selective delete in Elastic Beanstalk",
      "Make a snapshot of the database before it gets deleted",
      "Change the Elastic Beanstalk environment variables",
      "Convert the Elastic Beanstalk environment to a worker environment",
    ],
    correctAnswerIndex: 1,
    correctAnswerIndices: [1],
    isMultiSelect: false,
    explanation:
      "When an RDS database is 'coupled' to an Elastic Beanstalk environment (created as part of the environment), its lifecycle is tied to that environment. By default, terminating the Beanstalk environment will also terminate the RDS instance, leading to permanent data loss. To preserve the data for future exploration, you must ensure a snapshot is taken before deletion. In Elastic Beanstalk, you can configure the 'Database Deletion Policy' to 'Snapshot' or 'Retain'. If 'Snapshot' is selected, AWS will automatically create a final DB snapshot before the instance is deleted, which you can later restore to a new standalone database.",
    answerExplanations: [
      "Incorrect. Elastic Beanstalk does not have a 'selective delete' feature that allows you to choose specific resources to keep during environment termination; it is an all-or-nothing process unless the deletion policy is modified.",
      "Correct. Taking a snapshot (either manually or via the 'Snapshot' deletion policy) creates a persistent backup of the data that survives the environment's termination.",
      "Incorrect. Environment variables are used for application configuration (like database connection strings) and have no effect on the physical lifecycle or persistence of the underlying RDS resource.",
      "Incorrect. A worker environment is a specific type of Elastic Beanstalk environment designed for background tasks (using an SQS queue). Converting to this environment type does not decouple or protect the RDS database from the termination lifecycle.",
    ],
  },
  {
    id: "S11q54",
    domain: "Deployment",
    text: "A business-critical application is hosted on an Amazon EC2 instance and the latest update is in the testing phase. The business has requested a solution to track the average response time of the application and send a notification to the manager if it exceeds a particular threshold. The update will eventually be implemented in the production environment where the solution will be deployed on multiple EC2 instances.\n\nWhich of the following options would you combine to address the requirements of the business? (Select two)",
    answers: [
      "Configure the application to write the response time to a log file on the EC2 instance. Install and configure the Amazon CloudWatch agent on the EC2 instance to stream the application logs to CloudWatch Logs. Create a metric filter for the response time from the log file",
      "Configure an EventBridge custom rule to send an Amazon Simple Notification Service (Amazon SNS) notification when the average of the response time metric exceeds the threshold",
      "Create a CloudWatch alarm to send an Amazon Simple Notification Service (Amazon SNS) notification when the average of the response time metric exceeds the threshold",
      "Install and configure AWS Systems Manager Agent (SSM Agent) on the EC2 instances to monitor the response time and send the data to Amazon CloudWatch as a custom metric",
      "Configure the application to write the response time to a log file on the instance. Install and configure the Amazon Inspector agent on the EC2 instances to read the logs and send the response time to Amazon EventBridge",
    ],
    correctAnswerIndex: null,
    correctAnswerIndices: [0, 2],
    isMultiSelect: true,
    explanation:
      "To monitor application-level metrics like 'response time' that aren't available by default in CloudWatch, you must first ingest the data. The CloudWatch Agent can be used to push local application logs to CloudWatch Logs. Once the logs are in CloudWatch, a 'Metric Filter' can be applied to parse the logs for specific values (like the response time) and turn them into a numeric CloudWatch Metric. Finally, a CloudWatch Alarm is created to monitor this custom metric and trigger an SNS notification whenever the defined threshold is exceeded.",
    answerExplanations: [
      "Correct. This provides the mechanism to ingest custom application data and transform log entries into a measurable CloudWatch metric.",
      "Incorrect. While EventBridge can react to state changes in CloudWatch Alarms, it is not a tool for calculating averages of metrics over time or directly monitoring application performance thresholds.",
      "Correct. CloudWatch Alarms are the standard tool for monitoring metrics (like the average response time) and triggering actions like SNS notifications.",
      "Incorrect. The SSM Agent is used for remote management and configuration of instances (patching, shell access). It is not designed to parse application logs or create custom performance metrics.",
      "Incorrect. Amazon Inspector is a security vulnerability scanner. It does not possess the capability to read application logs for performance monitoring or push custom metrics to EventBridge.",
    ],
  },
  {
    id: "S11q55",
    domain: "Deployment",
    text: "A development team has a mix of applications hosted on-premises as well as on EC2 instances. The on-premises application controls all applications deployed on the EC2 instances. In case of any errors, the team wants to leverage Amazon CloudWatch to monitor and troubleshoot the on-premises application.\n\nAs a Developer Associate, which of the following solutions would you suggest to address this use-case?",
    answers: [
      "Upload log files from the on-premises server to S3 and let CloudWatch process the files from S3",
      "Configure CloudWatch Logs to directly read the logs from the on-premises server",
      "Configure the CloudWatch agent on the on-premises server by using IAM user credentials with permissions for CloudWatch",
      "Upload log files from the on-premises server to an EC2 instance which further forwards the logs to CloudWatch",
    ],
    correctAnswerIndex: 2,
    correctAnswerIndices: [2],
    isMultiSelect: false,
    explanation:
      "The Amazon CloudWatch agent is not limited to EC2 instances; it can also be installed on on-premises servers to collect system-level metrics and logs. Unlike EC2 instances, which use IAM Roles for permissions, on-premises servers require a set of IAM user credentials (access key and secret key) configured within the agent to authenticate with AWS and push data to CloudWatch Logs and Metrics.",
    answerExplanations: [
      "Incorrect. While you can upload logs to S3, CloudWatch does not natively 'process' log files from S3 to turn them into CloudWatch Logs streams without additional custom logic like Lambda. It is not the most direct solution.",
      "Incorrect. CloudWatch cannot 'pull' logs from a remote server. The data must be 'pushed' to the CloudWatch API, typically via the CloudWatch agent or the AWS CLI/SDK.",
      "Correct. Installing the CloudWatch agent on the physical server and providing it with IAM user credentials is the standard, supported method for integrating on-premises resources with CloudWatch.",
      "Incorrect. This architecture is unnecessarily complex and introduces a single point of failure (the EC2 instance) and extra latency compared to the agent sending data directly.",
    ],
  },
  {
    id: "S11q56",
    domain: "Development with AWS Services",
    text: "You would like to paginate the results of an S3 List to show 100 results per page to your users and minimize the number of API calls that you will use.\n\nWhich CLI options should you use? (Select two)",
    answers: [
      "--limit",
      "--next-token",
      "--page-size",
      "--max-items",
      "--starting-token",
    ],
    correctAnswerIndex: null,
    correctAnswerIndices: [3, 4],
    isMultiSelect: true,
    explanation:
      "To control pagination in the AWS CLI, you use `--max-items` and `--starting-token`. \n\n* `--max-items`: Specifies the total number of items to return in the output of the CLI command. If the total number of items available is greater than this value, the output includes a `NextToken` that you can use in a subsequent command.\n* `--starting-token`: Takes the `NextToken` from a previous call to resume pagination from where the last command left off.\n\nNote: `--page-size` is different; it controls how many items the CLI requests *per internal API call* but doesn't change the final output total.",
    answerExplanations: [
      "Incorrect. This is not a standard AWS CLI pagination parameter.",
      "Incorrect. While many AWS APIs return a field named 'NextToken', the CLI argument used to pass that token back in is actually `--starting-token`.",
      "Incorrect. While this affects how many items are fetched in each background API call to prevent timeouts, it does not limit the number of results displayed in the final output.",
      "Correct. This option limits the total number of items the CLI displays in the current response.",
      "Correct. This option allows you to pass a token from a previous response to fetch the next set (page) of results.",
    ],
  },
  {
    id: "S11q57",
    domain: "Troubleshooting and Optimization",
    text: "You are creating a web application in which users can follow each other. Some users will be more popular than others and thus their data will be requested very often. Currently, the user data sits in RDS and it has been recommended to use ElastiCache as a caching layer to improve read performance. The whole dataset of users cannot sit in ElastiCache due to cost, so you only want to cache the most often requested profiles. Stale data is acceptable for up to one minute.\n\nWhat caching strategy do you recommend implementing?",
    answers: [
      "Use a Lazy Loading strategy with TTL",
      "Use a Lazy Loading strategy without TTL",
      "Use a Write Through strategy with TTL",
      "Use a Write Through strategy without TTL",
    ],
    correctAnswerIndex: 0,
    correctAnswerIndices: [0],
    isMultiSelect: false,
    explanation:
      "Lazy Loading (also known as Cache-Aside) only loads data into the cache when a 'cache miss' occurs. This ensures that only frequently requested data (the popular profiles) occupies space in ElastiCache, which is more cost-effective than caching the entire database. Adding a Time to Live (TTL) of 60 seconds satisfies the requirement to ensure data is never more than a minute old, as the cache entry will expire and be refreshed from the database upon the next request.",
    answerExplanations: [
      "Correct. Lazy loading ensures only requested data is cached, and TTL manages the staleness of the data as required.",
      "Incorrect. Without a TTL, data in the cache would remain there indefinitely (or until evicted by the LRU algorithm), leading to highly stale data that exceeds the one-minute requirement.",
      "Incorrect. Write-through caches data every time a write occurs in the database. This would result in 'cache bloat,' storing every single user profile in the cache regardless of popularity, which exceeds the budget/storage constraints mentioned.",
      "Incorrect. Similar to the other write-through option, this would store all data in the cache and fail to manage the staleness requirement efficiently.",
    ],
  },
  {
    id: "S11q58",
    domain: "Development with AWS Services",
    text: "You would like to retrieve a subset of your dataset stored in S3 with the CSV format. You would like to retrieve a month of data and only 3 columns out of the 10.\n\nYou need to minimize compute and network costs for this, what should you use?",
    answers: ["S3 Access Logs", "S3 Inventory", "S3 Analytics", "S3 Select"],
    correctAnswerIndex: 3,
    correctAnswerIndices: [3],
    isMultiSelect: false,
    explanation:
      "S3 Select allows you to use simple SQL expressions to pull only the specific data you need from an object. Instead of downloading the entire CSV file (which consumes significant network bandwidth) and then filtering it in your application (which consumes compute resources), S3 Select performs the filtering on the S3 server side. This 'push-down' filtering drastically reduces the amount of data transferred over the network and the amount of data your application has to process, leading to lower costs and better performance.",
    answerExplanations: [
      "Incorrect. S3 Access Logs provide detailed records of requests made to your bucket; they do not filter the contents of the objects themselves.",
      "Incorrect. S3 Inventory provides a CSV or ORC file listing your objects and their metadata for auditing and reporting purposes, not for querying data within those objects.",
      "Incorrect. S3 Analytics is used to analyze storage access patterns to help you decide when to transition data to Infrequent Access (IA) storage classes.",
      "Correct. S3 Select is specifically designed to retrieve subsets of data (rows and columns) from S3 objects using SQL, minimizing both network and compute overhead.",
    ],
  },
  {
    id: "S11q59",
    domain: "Deployment",
    text: "One of your Kinesis Stream is experiencing increased traffic due to a sale day. Therefore your Kinesis Administrator has split shards and thus you went from having 6 shards to having 10 shards in your Kinesis Stream. Your consuming application is running a KCL-based application on EC2 instances.\n\nWhat is the maximum number of EC2 instances that can be deployed to process the shards?",
    answers: ["10", "1", "6", "20"],
    correctAnswerIndex: 0,
    correctAnswerIndices: [0],
    isMultiSelect: false,
    explanation:
      "In Amazon Kinesis Data Streams using the Kinesis Client Library (KCL), the number of shards is the determining factor for maximum parallelism. Each shard is processed by exactly one KCL worker (lease holder) at a time. Therefore, the maximum number of consumers (EC2 instances) you can have effectively working in parallel is equal to the number of shards in the stream. If you have more instances than shards, the extra instances will remain idle because there are no available shards to lease.",
    answerExplanations: [
      "Correct. Since there are 10 shards, you can have up to 10 EC2 instances, with each instance processing one shard.",
      "Incorrect. While one instance can process all 10 shards, it is not the *maximum* number of instances you can deploy for scaling purposes.",
      "Incorrect. 6 was the previous shard count. After the split, the new limit for parallelism is 10.",
      "Incorrect. You cannot have more active KCL workers than there are shards. 10 workers would handle the 10 shards, leaving 10 instances idle.",
    ],
  },
  {
    id: "S11q60",
    domain: "Deployment",
    text: "You would like to have a one-stop dashboard for all the CI/CD needs of one of your projects. You don't need heavy control of the individual configuration of each component in your CI/CD, but need to be able to get a holistic view of your projects.\n\nWhich service do you recommend?",
    answers: ["CodeDeploy", "CodePipeline", "CodeStar", "CodeBuild"],
    correctAnswerIndex: 2,
    correctAnswerIndices: [2],
    isMultiSelect: false,
    explanation:
      "AWS CodeStar is the 'project management' layer of the AWS developer tool suite. It provides a unified user interface that bundles CodeCommit, CodeBuild, CodePipeline, and CodeDeploy into a single dashboard. It is designed for developers who want to set up an entire continuous delivery toolchain quickly using templates, offering a holistic view of the development process—from code commits to deployment status and even Jira issue tracking—without needing to manually configure every underlying resource detail.",
    answerExplanations: [
      "Incorrect. CodeDeploy is a specialized service focused solely on automating code deployments to EC2, on-premises instances, Lambda, or ECS. It does not provide a holistic project dashboard.",
      "Incorrect. CodePipeline is an orchestration service for continuous integration and delivery. While it visualizes the 'flow' of code through various stages, it does not act as a unified dashboard for managing team access, project templates, or overall project management.",
      "Correct. CodeStar is the 'all-in-one' service that creates a project dashboard and integrates all individual CI/CD components into a single view.",
      "Incorrect. CodeBuild is a fully managed build service that compiles source code, runs tests, and produces software packages. It is just one component of a CI/CD pipeline, not a one-stop project dashboard.",
    ],
  },
  {
    id: "S11q61",
    domain: "Troubleshooting and Optimization",
    text: "A developer has created a new Application Load Balancer but has not registered any targets with the target groups.\n\nWhich of the following errors would be generated by the Load Balancer?",
    answers: [
      "HTTP 500: Internal server error",
      "HTTP 503: Service unavailable",
      "HTTP 502: Bad gateway",
      "HTTP 504: Gateway timeout",
    ],
    correctAnswerIndex: 1,
    correctAnswerIndices: [1],
    isMultiSelect: false,
    explanation:
      "When an Application Load Balancer (ALB) receives a request but finds that the associated target group has no registered targets, or if all registered targets are currently unhealthy, it returns an 'HTTP 503: Service unavailable' error. This indicates that the load balancer itself is functioning, but it has no backend capacity to handle the request.",
    answerExplanations: [
      "Incorrect. An HTTP 500 error is typically generated by the application/backend server itself when it encounters an unexpected condition, not by the load balancer due to missing targets.",
      "Correct. This is the specific error code returned when there are no registered targets in a target group.",
      "Incorrect. An HTTP 502 error occurs when the load balancer receives an invalid response from the backend target (e.g., the target closed the connection prematurely or sent a malformed header).",
      "Incorrect. An HTTP 504 error is a timeout error. This happens when the load balancer establishes a connection with the target, but the target fails to respond within the configured idle timeout period.",
    ],
  },
  {
    id: "S11q62",
    domain: "Deployment",
    text: "As part of your video processing application, you are looking to perform a set of repetitive and scheduled tasks asynchronously. Your application is deployed on Elastic Beanstalk.\n\nWhich Elastic Beanstalk environment should you set up for performing the repetitive tasks?",
    answers: [
      "Setup a Web Server environment and a .ebextensions file",
      "Setup a Web Server environment and a cron.yaml file",
      "Setup a Worker environment and a .ebextensions file",
      "Setup a Worker environment and a cron.yaml file",
    ],
    correctAnswerIndex: 3,
    correctAnswerIndices: [3],
    isMultiSelect: false,
    explanation:
      "To handle asynchronous, background, or scheduled tasks in Elastic Beanstalk, the 'Worker Environment' is the correct choice. It includes a specialized daemon that pulls messages from an SQS queue and posts them to your application. For scheduling repetitive tasks (like a cron job), you include a `cron.yaml` file in your source bundle. Elastic Beanstalk uses this file to trigger periodic tasks by sending a message to the worker's SQS queue at specified intervals.",
    answerExplanations: [
      "Incorrect. A Web Server environment is designed to handle HTTP requests from users, not for asynchronous background processing. While `.ebextensions` can configure a Linux cron, it is not the native or scalable way to handle periodic tasks in Beanstalk.",
      "Incorrect. The `cron.yaml` file is a feature specifically designed for Worker environments; it does not function in a Web Server environment.",
      "Incorrect. While a Worker environment is the correct tier, `.ebextensions` is used for general configuration (like installing software or environment variables). The specific mechanism for scheduling repetitive tasks in a Worker tier is the `cron.yaml` file.",
      "Correct. This combination leverages the Worker tier's SQS-based processing and the native `cron.yaml` scheduling functionality.",
    ],
  },
  {
    id: "S11q63",
    domain: "Development with AWS Services",
    text: "Your Lambda function processes files for your customers and as part of that process, it creates a lot of intermediary files it needs to store on its disk and then discard.\n\nWhat is the best way to store temporary files for your Lambda functions that will be discarded when the function stops running?",
    answers: [
      "Use the local directory /opt",
      "Create a tmp/ directory in the source zip file and use it",
      "Use the local directory /tmp",
      "Use an S3 bucket",
    ],
    correctAnswerIndex: 2,
    correctAnswerIndices: [2],
    isMultiSelect: false,
    explanation:
      "Each AWS Lambda function provides a temporary file system available at the `/tmp` directory. This is local storage for the execution environment. By default, Lambda provides 512 MB of disk space in this directory, though this can be increased up to 10 GB. It is intended for ephemeral data—intermediary files created during processing that do not need to persist beyond the lifecycle of the function execution. Note that while data may persist across 'warm' starts (re-using the same execution environment), it is not guaranteed and should always be treated as temporary.",
    answerExplanations: [
      "Incorrect. The `/opt` directory is reserved for Lambda Layers. It is read-only during function execution and cannot be used for writing temporary intermediary files.",
      "Incorrect. The source code in the deployment package (zip file) is mounted as read-only. You cannot write new files into a directory that was packaged within the zip file during runtime.",
      "Correct. The `/tmp` directory is the designated writable space for ephemeral storage on the Lambda execution environment's local disk.",
      "Incorrect. While S3 can store files, it is remote storage. Using it for small intermediary files is slower (network latency) and more expensive (API call costs). Additionally, files in S3 persist until manually deleted, whereas the goal here is to use temporary space that is discarded.",
    ],
  },
  {
    id: "S11q64",
    domain: "Development with AWS Services",
    text: "A media company wants to migrate a video editing service to Amazon EC2 while following security best practices. The videos are sourced and read from a non-public S3 bucket.\n\nAs a Developer Associate, which of the following solutions would you recommend for the given use-case?",
    answers: [
      "Set up an S3 service role with read-only permissions for the S3 bucket and attach the role to the EC2 instance profile",
      "Set up an IAM user with read-only permissions for the S3 bucket. Configure AWS credentials for this user via AWS CLI on the EC2 instance",
      "Set up an IAM user with read-only permissions for the S3 bucket. Configure the IAM user credentials in the user data of the EC2 instance",
      "Set up an EC2 service role with read-only permissions for the S3 bucket and attach the role to the EC2 instance profile",
    ],
    correctAnswerIndex: 3,
    correctAnswerIndices: [3],
    isMultiSelect: false,
    explanation:
      "According to AWS security best practices, you should never store long-lived credentials (like IAM User Access Keys) on an EC2 instance. Instead, you should use IAM Roles. An IAM Role for EC2 allows the instance to automatically retrieve temporary security credentials from the Instance Metadata Service (IMDS). To implement this, you create an IAM Role with a trust policy allowing the 'ec2.amazonaws.com' service to assume it, attach the necessary permissions (S3 Read-Only), and then assign that role to an Instance Profile which is attached to the EC2 instance.",
    answerExplanations: [
      "Incorrect. While the permissions are correct, the 'Service Role' must be for the service that will *assume* the role (EC2), not the service being accessed (S3).",
      "Incorrect. Storing IAM user credentials on an instance (even via the CLI) is a security risk because those credentials do not expire and could be compromised if the instance is accessed by unauthorized users.",
      "Incorrect. User data is often stored in plain text and can be easily retrieved via the instance metadata. Hardcoding credentials in user data is a significant security vulnerability.",
      "Correct. Using an EC2 service role attached via an instance profile is the most secure method, as it uses temporary, automatically rotated credentials.",
    ],
  },
  {
    id: "S11q65",
    domain: "Development with AWS Services",
    text: "An e-commerce company has multiple EC2 instances operating in a private subnet which is part of a custom VPC. These instances are running an image processing application that needs to access images stored on S3. Once each image is processed, the status of the corresponding record needs to be marked as completed in a DynamoDB table.\n\nHow would you go about providing private access to these AWS resources which are not part of this custom VPC?",
    answers: [
      "Create a gateway endpoint for DynamoDB and add it as a target in the route table of the custom VPC. Create an API endpoint for S3 and then connect to the S3 service using the private IP address",
      "Create a gateway endpoint for S3 and add it as a target in the route table of the custom VPC. Create an interface endpoint for DynamoDB and then connect to the DynamoDB service using the private IP address",
      "Create a separate interface endpoint for S3 and DynamoDB each. Then connect to these services using the private IP address",
      "Create a separate gateway endpoint for S3 and DynamoDB each. Add two new target entries for these two gateway endpoints in the route table of the custom VPC",
    ],
    correctAnswerIndex: 3,
    correctAnswerIndices: [3],
    isMultiSelect: false,
    explanation:
      "To connect to AWS services privately from a VPC without using an Internet Gateway or NAT Gateway, you use VPC Endpoints. There are two types: Gateway and Interface. S3 and DynamoDB are the only two services that support 'Gateway Endpoints'. These are configured by adding a specific route to your VPC route table that points traffic destined for the service prefix list (e.g., pl-63a5400a for S3) to the VPC Endpoint ID. While S3 recently added support for Interface Endpoints (PrivateLink), DynamoDB still only supports Gateway Endpoints. Therefore, the most standard and cost-effective way to provide private access to both simultaneously is by creating a Gateway Endpoint for each.",
    answerExplanations: [
      "Incorrect. There is no such thing as an 'API endpoint' for S3 in the context of VPC networking. Furthermore, you cannot connect to S3 via a 'private IP address' using a Gateway Endpoint; you continue to use the public DNS name, which the route table then redirects.",
      "Incorrect. DynamoDB does not support Interface Endpoints (PrivateLink); it only supports Gateway Endpoints.",
      "Incorrect. As noted, DynamoDB does not support Interface Endpoints. While S3 does support them, using Interface Endpoints for S3 incurs hourly and data-processing costs, whereas Gateway Endpoints for S3 are free.",
      "Correct. Creating a Gateway Endpoint for both S3 and DynamoDB and updating the route table is the standard architectural pattern for private access to these two specific services.",
    ],
  },
];